# 🧠 ELLORA PROJECT MEMORY
> Last Updated: August 14, 2026
> Read this file at the start of every conversation to understand project context.
> **Standing rule**: update this file after every meaningful change (not just at session end) —
> add what changed, why, and what's still not built, the same level of detail as existing entries.
> Don't leave it to go stale, and don't leave a superseded note uncorrected (see html/ note below —
> it was wrong for months before anyone fixed it).

---

## 📌 PROJECT OVERVIEW

**What**: Ellora Fashion & Lifestyle Store — a ThemeForest HTML template being converted into a fully dynamic e-commerce platform with an enterprise-grade admin panel (25 modules).

**Where**: `c:\Users\DELL-G3 15-3500\Downloads\themeforest-erJI4BDo-ellora-fashion-lifestyle-store-html-template\`

**Goal**: Build a production-grade admin panel at `/admin` with 25 modules covering products, orders, inventory, CRM, marketing, finance, AI, and more — starting with 8 essential modules first.

**Vision**: Shopify-level admin panel that can scale to handle lakhs of data points.

---

## ▶️ HOW TO RUN

**First time only** — install dependencies for root, `server/`, and `admin/`:
```bash
npm run install:all
```

**Run everything together** (storefront + Express API + admin panel):
```bash
npm run dev:full
```

Or run pieces individually, in separate terminals:
```bash
npm run dev         # storefront only  -> http://localhost:3000
npm run dev:server  # Express API only -> http://localhost:4000
npm run dev:admin   # admin panel only -> http://localhost:5173
```

**Admin panel login**: open `http://localhost:5173/login` and sign in with:
- Email: `admin@ellora.test`
- Password: `EllroaAdmin@2026`

**Requires** `server/.env` and `admin/.env` to have real Supabase keys (already filled in — see NEXT ACTION section for what's in them). If either dev server won't start, check nothing else is already using ports 4000/5173/3000.

**Storefront port can vary**: `browser-sync` defaults to port 3000, but if 3000 is occupied it auto-picks the next free port (3001, 3002, …). The Express server's CORS config (`server/index.js`) now uses a dynamic regex that allows **any localhost port**, so this is no longer a problem. If you see "Failed to fetch" on the storefront, the issue is the Express API server not running — not a port mismatch.

---

## 🛠️ CONFIRMED TECH STACK (Hybrid Approach)

| Layer | Technology | Why |
|-------|-----------|-----|
| **Backend API** | Express.js | Simple, flexible, full control |
| **Frontend** | React + Vite | Modern SPA for admin panel |
| **UI Components** | Shadcn/ui + Tailwind CSS | Premium component library |
| **Database** | Supabase (cloud PostgreSQL) | Scalable, free tier, built-in features |
| **Auth** | Supabase Auth | 2FA, SSO, session management, RBAC |
| **File Storage** | Supabase Storage | Cloud buckets for images/videos |
| **Realtime** | Supabase Realtime | Live order updates, stock alerts |
| **Image Storage** | Cloudinary | Image optimization & CDN |

### Previous Stack (ABANDONED)
~~Express + EJS + SQLite + bcrypt~~ → replaced with the hybrid approach above.

---

## ✅ WHAT HAS BEEN COMPLETED

### 1. Static Template Setup (Done ✅)
- Explored the full template structure (29 HTML pages, CSS, JS, fonts, images)
- Set up `browser-sync` dev server to run the template locally at `http://localhost:3000`
- Created `package.json` with dev scripts

### 2. Planning & Documentation (Done ✅)
- Created Backend Architecture Report (Word doc with 8 diagrams): `Ellora_Backend_Architecture_Report.docx`
- Analyzed reference documents: `Building a production Admin Panel.docx` + `Required key components in Admin panel.docx`
- Mapped out all 25 modules and prioritized into phases

### 3. Key Decisions (Confirmed ✅)
| Decision | Answer |
|----------|--------|
| Stack | Hybrid — Express API + React/Shadcn + Supabase |
| Database | Supabase (cloud PostgreSQL, free tier) |
| Auth | Supabase Auth |
| File storage | Supabase Storage + Cloudinary |
| First release scope | 8 essential modules |
| Full vision | 25 modules (build incrementally) |
| admin accounts | Single admin first, expand to 7 roles later |

### 4. Storefront Customer Portal & Authentication (Done ✅)
- **Auth Infrastructure**: Created [auth.js](file:///c:/Users/ADMIN/Downloads/Saint-Roman/html/js/auth.js) to wrap Supabase Client functionality (register, login, session management, and `apiFetch`).
- **Backend API**: Implemented [customer.js](file:///c:/Users/ADMIN/Downloads/Saint-Roman/server/routes/customer.js) to securely handle customer-specific data (profile details/updates, password changes, address book CRUD, order history, and wishlist).
- **Account HTML/JS Pages**: Integrated dynamic scripts (`account-*.js` files in [js/](file:///c:/Users/ADMIN/Downloads/Saint-Roman/html/js/)) and linked them with customer storefront HTML dashboards (`account-*.html` files in [html/](file:///c:/Users/ADMIN/Downloads/Saint-Roman/html/)) for real-time customer data presentation.

### 5. Storefront Compilation & Optimization (Done ✅)
- Created a custom build script [build-html.js](file:///c:/Users/ADMIN/Downloads/Saint-Roman/scripts/build-html.js) that copies static assets, minifies HTML, and extracts and minifies inline scripts to `dist/js/generated/` to optimize load times and compile the storefront for production.

### 6. Dynamic Storefront Assets (Done ✅)
- Connected storefront homepage hero, mega menu categories/tags, and product listings to fetch dynamically from live database entities rather than using static mock assets.

### 7. Barcode Generation, Scanning & Printing (Done ✅)
- **Code128 Generation Logic**: Implemented [barcode.js](file:///c:/Users/ADMIN/Downloads/Saint-Roman/server/lib/barcode.js) to programmatically generate unique Code128 barcodes prefixed with `ELR` followed by 10 digits.
- **Scanner & Renderer Components**: Created a barcode scanner page [BarcodeScannerPage.tsx](file:///c:/Users/ADMIN/Downloads/Saint-Roman/admin/src/pages/BarcodeScannerPage.tsx) and inline renderer [BarcodeSvg.tsx](file:///c:/Users/ADMIN/Downloads/Saint-Roman/admin/src/components/shared/BarcodeSvg.tsx) using the `JsBarcode` framework.
- **Label Printer Utility**: Developed [barcodePrint.ts](file:///c:/Users/ADMIN/Downloads/Saint-Roman/admin/src/lib/barcodePrint.ts) to construct and prompt printable barcode layouts within separate web context windows.
- **Backfill Script**: Provided a command line script [backfill-barcodes.js](file:///c:/Users/ADMIN/Downloads/Saint-Roman/server/scripts/backfill-barcodes.js) to retroactively populate missing barcodes for historical products.

### 8. Storefront Polish & Blog Wiring (Done ✅ — 2026-08-11, branch `claude/project-review-changes-yjarew`, not yet merged to `main`)
- **Account sidebar**: removed/commented out the "Downloads" link from the account sidebar `<ul>` across all 8 `account-*.html` pages (digital-downloads isn't a real feature here — physical fashion/lifestyle store).
- **Blog wired to real data end-to-end**:
  - `admin/src/pages/BlogPostsPage.tsx` — added featured-image upload (reuses the existing `ImageUpload`/Cloudinary flow), thumbnail column in the posts table.
  - `server/routes/public.js` — new `GET /api/public/blog-posts` (published-only, paginated) and `GET /api/public/blog-posts/:slug` (full post).
  - `server/routes/cms.js` — auto-dedupes `blog_posts.slug` on create/update (appends `-2`, `-3`, ...) instead of raw-erroring on `blog_posts_slug_key` when two posts share a title.
  - `html/blog.html` — hidden-template-card + fetch pattern, replaces the 6 hardcoded posts.
  - `html/blog-single.html` / new `html/js/blog-single-dynamic.js` — fetches by `?slug=`, renders title/date/image/content. Plain-text `content` (admin's Content field is a plain `<Textarea>`, no rich text) is reformatted into real `<p>`/`<ul><li>` blocks instead of one run-on paragraph, via the new shared `html/js/text-format.js` (`EllroaText.render`/`formatPlainText`/`looksLikeHtml`/`escapeHtml`) — extracted here so `product-single-dynamic.js` could reuse the exact same formatter instead of copy-pasting it (see below). List rule: any block of 2+ consecutive non-blank lines is a real `<ul><li>` list, marker optional (`1.`/`-`/`*`/none all work) — one rule covers both the blog's numbered lines and a marker-less feature list. **Real bug fixed**: the theme's `.text-anime-style-3` heading-reveal animation (`js/function.js`) splits+animates whatever text is in the `<h1>` on page load; calling `SplitText.revert()` *after* writing the real title silently reverted it back to the static placeholder — fixed the ordering (revert → set real text → re-split/animate).
  - Homepage "Follow us for daily style" section: consolidated into `html/js/homepage-dynamic-sections.js` (`loadBlogPosts()`) instead of its own duplicate inline `fetch`, matching every other section in that file (categories, new arrivals, offers, featured products, collections, promo banners).
- **Header nav reordered** across all 28 `html/*.html` pages: `Home, Shop, Pages, Contact Us, My Account` — Blog is no longer top-level, it's the first item inside the Pages dropdown.
- **`product-single.html` fixed** (was still 100% static demo content behind a live title/price/description fetch):
  - **Image**: `GET /api/public/products/:slug` was selecting the unused `product_images` gallery table instead of `products.image_url` (the column the admin's image upload actually writes to, added in `phase6_banner_image.sql`) — added it to the select. One image per product (no real gallery), applied to every slide in both the main slider and thumbnail strip.
  - **Currency**: was `'$' + toFixed(2)` here while `products.html` correctly used `₹`. New shared `html/js/currency.js` (`EllroaCurrency.format`) — wired into product-single, cart, checkout, `account-wishlist.js`, `account-orders.js`, `homepage-dynamic-sections.js`, `products.html` (was 5+ independent copies/hand-rolled `$`). Admin side was already correct ₹ everywhere; deduped its one real copy-paste (`formatCurrency` in `DashboardPage.tsx` + `FinancePage.tsx`) into `admin/src/lib/currency.ts`.
  - **Description/Additional Information/Reviews tabs** were the same static "Chic Aura Crop Top" copy on every product regardless of which one was open. Description now renders `products.description` (real column, honest "No description available yet." if empty) — **also went through the `EllroaText`/`text-format.js` plain-text formatter** above (first version wrapped the whole description in one `<p>`, which collapsed the admin's blank-line paragraphs and feature-list lines into one run-on block; fixed same day). Reviews honestly shows "No reviews yet" / "Reviews (0)" — **no reviews table exists anywhere in the schema**, so this was fake data with no backing feature; not rebuilt, just made honest.
  - **Related products**: were 4 hardcoded products on every page. Now reuses the existing `GET /products?category=<slug>` endpoint (same one `products.html`'s main grid uses) — same category as the current product, excludes itself, shows however many are actually available (0–4). No new endpoint needed.
  - Extracted the inline `<script>` into `html/js/product-single-dynamic.js`, matching the project's dynamic-page convention (`blog-single-dynamic.js`, `mega-menu-dynamic.js`, `homepage-dynamic-sections.js`).
- **New: Product Specifications** (`products.specifications jsonb`, `server/supabase/phase8_product_specifications.sql`, registered in `server/scripts/migrate.js`) — the real backing field for the storefront's "Additional Information" tab. JSONB chosen over fixed columns (material/fit/neckline only fit apparel; Ellora also sells watches/beauty/accessories with different attributes) or a separate table, matching existing JSONB precedent in this schema (`orders.shipping_address`, `audit_logs.details`, `settings.payment_gateways`) and, like `tags`, slotting into the product form's *existing* single save payload. Admin: new "Additional Information" add/remove label-value row section in `ProductsPage.tsx`'s edit form, right under Description. Storefront: `product-single-dynamic.js` renders the real pairs or "No additional information available" if none are set. **Not yet run against the live Supabase instance from this session** — needs `npm run migrate` (or the SQL Editor) before it'll actually work.
- **`account-order-details.html` wired to the real order** — it was loading `account-dashboard.js` (wrong script, a copy-paste leftover) and never read `?id=` at all, so every order showed the same static demo data ("Sophia Brown", a London address, one fake line item, and a "Tax: ₹105" line with no backing column on `orders`). New `html/js/account-order-details.js` uses the customer API that already existed for this (`server/routes/customer.js` `GET /api/customer/orders/:id`, already ownership-checked) — real line items, subtotal/shipping/discount/total via `EllroaCurrency`, payment method; dropped the fake Tax line (no tax column on `orders`). Both Billing and Shipping sections render the same real `orders.shipping_address` snapshot, since `checkout.html` only ever collects one address — no separate billing form exists to pull a different one from. **Accepted limitation** (same as cart.html/checkout.html): `order_items` has no product-image column, so line items use the same generic placeholder image those two pages already use for the same reason.
- **`order-received.html` wired to the real order too** (same day, after actually placing a real order and seeing the demo data first-hand) — only its "ORDER ..." heading read `?order=`; everything else (date, payment method, total, billing/shipping, line items, subtotal/shipping/total, and a fabricated "Tax: ₹350" line with no backing column) was the same static template content on every checkout. Checkout is **guest** (no login), so this couldn't reuse the authenticated customer-orders endpoint `account-order-details.html` uses — new **public** `GET /api/public/orders/:orderNumber` in `server/routes/public.js` (unauthenticated, looked up by `order_number` — an unguessable timestamp+random string, not the uuid `id` — matching what `checkout.html` actually gets back and puts in the URL). New `html/js/order-received-dynamic.js` renders it the same way `account-order-details.html` does (dropped the fake Tax line, both address sections show the same real `shipping_address` snapshot).
- **`cart.html` "You May be interested:"** was 3 hardcoded products (Chic Aura Crop Top / Velvet Charm Gown / Elegant Flow Kurti) with broken placeholder images, identical regardless of cart contents. Cart line items (`js/ellora-cart.js`) only store variantId/productSlug/productName/variantLabel/price/quantity — no category — so rather than add new "related to cart" server logic, it reuses the exact same featured-products-with-newest-fallback query `homepage-dynamic-sections.js`'s `loadFeaturedProducts` already uses (`GET /products?tag=featured`, falls back to `?sort=newest`), excluding whatever product slugs are already in the cart. Mutates the 3 existing cards in place (same pattern as `product-single-dynamic.js`'s related products), hides extra cards / the whole section if nothing's left to recommend.
- **🔴 Real checkout was broken — fixed, in two rounds**: `server/routes/public.js`'s checkout (`POST /api/public/orders`) turned out to have **three** places where it inserted into columns/RPCs that no migration had ever actually created — the whole handler had apparently never been tested against the real deployed schema (testing had only gone through the admin's own order-creation dialog, a different code path). Found by actually clicking "Place Order" and reading each schema-cache error in turn, fixing, retrying:
  1. `orders.payment_method` / `orders.payment_status` — `Could not find the 'payment_method' column of 'orders' in the schema cache`. Fixed in `server/supabase/phase9_order_payment_method.sql`.
  2. `customers.address` — same error pattern, next: `Could not find the 'address' column of 'customers' in the schema cache`. `customers` (`phase4_customers.sql`) only ever had id/name/email/phone/notes.
  3. `decrement_stock(uuid, int)` RPC — found while auditing the rest of the same handler for more of the same bug class, not from a user-visible error, since it's wrapped in `.catch(() => {})`. **Every real storefront checkout order has placed successfully without ever actually decrementing stock** — silently broken since real checkout was first built. Admin-created orders (`server/routes/orders.js`) decrement stock a different way (direct update, not this RPC), so that path was unaffected.
  4. `duplicate key value violates unique constraint "customers_email_idx"` — a real logic bug, not a schema gap. The customer-upsert step looked up "existing customer" by **phone only**, but `customers`' actual unique constraint (`phase4_customers.sql`) is a **partial** unique index **on email** (`where email is not null`) — phone isn't unique-constrained at all. A phone-only `.maybeSingle()` lookup silently fails (no match, no error surfaced) if more than one row shares that phone — which had already happened, from the customer-insert succeeding on earlier failed attempts above before the *order* insert then failed on #1/#2. The lookup missed the existing row and fell through to insert, colliding on email.
     - **First fix attempt failed**: `.upsert(..., { onConflict: 'email' })` — threw `there is no unique or exclusion constraint matching the ON CONFLICT specification`. Root cause: Postgres requires an `ON CONFLICT` target to exactly match a constraint/index *including its predicate*, and `customers_email_idx` is partial (`where email is not null`); `supabase-js`'s `.upsert()` has no way to express that partial predicate in `onConflict`, so this was never going to work — a real client-library/Postgres limitation, not a transient error worth retrying as-is.
     - **Actual fix**: reverted to select-then-insert (no `ON CONFLICT` involved, so the partial-index limitation doesn't apply at all), keyed on **email** — the column that's actually constrained — instead of phone, with phone only as a fallback when email is missing (the server itself doesn't require it, only the form does).
  5. **Server crash** (Node process exit, not an HTTP error) — `TypeError: supabaseAdmin.rpc(...).catch is not a function`, once request #4's fix let execution reach further into the handler than any previous attempt had. This was **pre-existing code**, not introduced by any of the fixes above — `supabaseAdmin.rpc()`/`.from()` return a `PostgrestFilterBuilder`, which is *thenable* (`await` works) but is **not a real Promise** and has no `.catch()`/`.finally()`. Two call sites right next to each other (`decrement_stock` RPC, and the coupon `usage_count` update) both chained `.catch(() => {})` straight onto the builder instead of awaiting first — crashes the instant either runs, outside any try/catch, killing the whole server (that's why the *next* attempt showed `ERR_CONNECTION_REFUSED` — nothing was listening on :4000 anymore until the server was manually restarted). Audited all of `server/` for the same pattern: no other real instances — `gemini.js`'s `res.json().catch()` is on a `fetch()` Response (real Promise) and `auth.js`'s `signOut(...).catch()` is on the Auth Admin API (also a real Promise), both correct as-is. Fixed both real call sites to await and check `{ error }` instead.
  #2 and #3 fixed together in `server/supabase/phase10_checkout_schema_gaps.sql`; #4 and #5 needed no migration, application logic only (#4 took two commits — the `onConflict` attempt, then the real fix). Both new migrations registered in `server/scripts/migrate.js`. **This handler had 5 real bugs stacked on top of each other, found one at a time by actually clicking "Place Order" repeatedly — if it throws yet another error, keep going the same way (read the actual error, verify against actual code, don't guess) rather than assuming it's fully clean now.**
  - **Also fixed while debugging this live**: `npm run migrate` didn't actually exist — `server/package.json` had no `"migrate"` script, only `dev`/`start`; `server/scripts/migrate.js` had to be run directly (`node scripts/migrate.js`). Added the script alias so `npm run migrate` (referenced throughout this file, including right above) actually works.
  - Also made **Postcode/ZIP required** in `checkout.html` (was the only address field without `required`, explicitly labeled "(optional)") — matches `address`/`city`/`district`/`country`, all already required. Client-side only (`form.reportValidity()` already gates submission), same validation depth the other address sub-fields have — not enforced server-side either, consistent with them rather than singled out.

### 9. Admin Orders → full ops console (Done ✅ — 2026-08-11, branch `claude/project-review-changes-yjarew`, not yet merged to `main`, migration not yet run)

Full redesign of the admin Orders panel from a flat CRUD table into a Myntra Seller Hub / Flipkart Seller Central–style ops console, per an explicit detailed spec. **Both `server/routes/orders.js` and `admin/src/pages/OrdersPage.tsx` were complete rewrites** (not diffs), per the request.

- **Data model** — new `server/supabase/phase11_orders_ops.sql` (registered in `server/scripts/migrate.js`, alongside phase8/9/10): adds `orders.shipped_at` / `orders.delivered_at` (timestamptz), plus indexes on `payment_status` and `courier`. `payment_status`, `tracking_number`, `courier`, `notes` already existed from earlier phases (9/10) and the original orders schema — only the two fulfilment timestamps and the two indexes were actually new. **Not yet run against the live Supabase instance** — needs `npm run migrate` or the SQL Editor before the new columns exist.
- **Status state machine** — replaced free-form "set any status" with a `NEXT_STATUSES` map (`pending→packed/cancelled`, `processing→packed/cancelled`, `packed→ready_to_ship/shipped/cancelled`, `ready_to_ship→shipped/cancelled`, `shipped→delivered/returned`, `delivered→returned`, `cancelled→[]`, `returned→refund_initiated`, `refund_initiated→refund_completed`, `refund_completed→[]`). Enforced server-side on `PUT /api/orders/:id` and the new `PATCH /api/orders/bulk`; the admin's status dropdown only ever offers valid next-states so an invalid transition can't be attempted from the UI either. **This map is duplicated by hand in both files** (`server/routes/orders.js` and `admin/src/pages/OrdersPage.tsx`) — there's no shared package between server and admin in this project, so if the state machine ever changes, update both.
- **Computed fields** (`decorateOrder()` in `server/routes/orders.js`, applied to every order the list/detail endpoints return):
  - `sla_breached` — `true` if `status === 'pending'` and `created_at` is >24h old, or `status === 'packed'` with no `shipped_at` and `updated_at` >24h old (there's no `packed_at` column — only the two real one-way-transition timestamps from phase11 — so `updated_at` is the best available proxy for "how long has it been packed").
  - `high_value` — `total >= 3000` (a **fixed** ₹ threshold, `HIGH_VALUE_THRESHOLD` in `orders.js`, chosen deliberately over a live "top 10% of orders" percentile — a percentile shifts under ops staff's feet as new orders come in, so a fixed number is the more stable/legible signal, matching how real seller-ops tools do this).
  - `order_items[].image_url` — flattened from the nested `order_items → product_variants → products` join so the client gets a real per-line-item thumbnail without doing its own extra fetch.
- **Filtering** — `applyFilters()` handles `date_from`/`date_to`, `payment_status`, `min_total`/`max_total`, `city`/`pincode` (via PostgREST's `shipping_address->>city` / `->>pincode` jsonb text-extraction, since `shipping_address` is jsonb, not separate columns), `courier`, and `search` (ilike `.or()` across order_number/customer_name/customer_phone/tracking_number). Deliberately **excludes** `status` so it can be reused for both the main list query and the per-tab counts — the status pill tabs' counts reflect "how many X-status orders match the other active filters," the same way Flipkart/Myntra seller-hub tab counts behave, not a global unfiltered count.
- **New endpoints**: `GET /api/orders/couriers` (distinct non-null courier list, for the courier filter dropdown), `PATCH /api/orders/bulk` (body `{ ids, status }` — validates each order's *own* current status individually against `NEXT_STATUSES` rather than rejecting the whole batch over one bad transition, since a mixed selection can be at different stages; returns `{ updated: [...], failed: [{id, order_number, reason}] }` so the client can toast partial failures). Both declared **before** `GET /:id` in the route file so the literal sub-paths aren't swallowed by the `:id` param route — same convention already used for `/barcode/:code` in `products.js`.
- **Status timeline** — `GET /api/orders/:id` now also returns a `timeline` array, sourced from the **existing** `audit_logs` table (phase5) filtered to `entity_type='order'`/`entity_id=<id>`/`action='status_change'` — both `PUT /:id` and `PATCH /bulk` already called `logAudit()` on every real status change before this task, so no new table was needed, just a new read of an existing one.
- **Admin UI** (`admin/src/pages/OrdersPage.tsx`, ~1240 lines): toolbar (search, date presets + range, payment-status/courier `Select`s, min/max ₹, city/pincode, Export CSV, New order — the pre-existing New-order dialog logic was carried over unchanged); status pill tabs with live counts; dense table (row-select checkboxes, order # with relative time + a star for high-value, customer, stacked overlapping item thumbnails with a native `title` tooltip for the full list, payment badge, total, a status `Select` limited to `NEXT_STATUSES[order.status]`, tracking cell with a courier icon, row actions dropdown); SLA-breached rows get a subtle destructive-tinted background; a confirmation `Dialog` gates any transition into Cancelled/Refund-initiated/Refund-completed; a right-side `Sheet` drawer (line items with images, totals breakdown, shipping address, payment badge, status timeline, an editable notes `Textarea` with its own Save button, print-invoice/print-shipping-label buttons); a floating bulk-action bar appears once ≥1 row is selected (bulk status change, generate labels, export selected, cancel); server-side pagination with a page-size `Select`; skeleton rows while loading; distinct empty-state copy per active tab.
- **New shared UI primitives** (constraint: reuse existing `shadcn`/Base UI conventions, don't introduce a new UI library):
  - `admin/src/components/ui/checkbox.tsx` — a plain native `<input type="checkbox">`, not a new `@base-ui/react/checkbox` import. Every other primitive in this folder wraps a Base UI component, but row-selection needs nothing beyond the browser's own checkbox semantics (including real `indeterminate` state for "some rows on this page selected"), so this avoids depending on a Base UI subpath that's unproven anywhere else in the codebase.
  - `admin/src/components/ui/sheet.tsx` — a right-edge slide-over drawer built on the **same** `@base-ui/react/dialog` primitive `dialog.tsx` already uses, just repositioned/animated as a panel pinned to the viewport edge instead of a centered modal (the same relationship shadcn/ui's own `Sheet` has to its `Dialog`) — so it's not a new dependency, just a new composition of a proven one.
  - `admin/src/lib/orderPrint.ts` — `printOrderInvoice()`/`printShippingLabel()`/`printShippingLabels()`, modeled directly on the existing `admin/src/lib/barcodePrint.ts` pattern (`window.open()` + `document.write()` + `window.print()`). There's no PDF or zip library anywhere in this project, so "Save as PDF" via the browser's own print dialog is the actual mechanism — `printShippingLabels()` for the bulk "Generate labels" action just concatenates every selected order's label into one print window with `page-break-after` between them, an honest batch rather than a fabricated server-side zip.
- **Convention followed**: `Select` filters use the literal string `"all"` as the "no filter" sentinel (not `""`) — matches the established pattern already in `ProductsPage.tsx` (`categoryFilter`/`statusFilter`), confirmed by reading that file before assuming rather than guessing, per the standing instruction to verify against real code first.
- **Auth/RBAC**: every route in `orders.js`, old and new, still runs behind the existing `requireAuth` middleware — no new role restrictions were added, matching the explicit "preserve existing auth/RBAC" constraint.
- ~~**Known pre-existing gap, unrelated to this task, not fixed here**: the "New order" dialog's "Existing customer" dropdown (unchanged carry-over from the previous version of this page) calls `GET /api/customers` — but `server/routes/customers.js` (mounted at `/api/customers`) is actually the **storefront** customer-account router (`requireCustomer` JWT middleware, routes like `/profile`/`/orders`/`/addresses`/`/wishlist`) and has no bare `GET /`/`POST /` at all. The admin CRM customer-list-with-`order_count`/`lifetime_value` logic that `CustomersPage.tsx` also expects at this same path only exists as an internal helper (`loadCustomersWithStats()`) inside `server/routes/crm.js` (mounted at `/api/crm`, only exposing `/segments`), not as its own endpoint anywhere. This means the "Existing customer" picker (and `CustomersPage.tsx` itself) likely 404s today — pre-existing, not a regression from this rewrite, and out of scope for the Orders task; flagging it here since it'll otherwise look like a new bug next time someone touches Orders or Customers.**~~ **Fixed 2026-08-14 — see section 17.**
- **Not verified in this sandbox**: `admin/node_modules` isn't installed here, so none of the new/changed admin TypeScript could be run through `tsc`/`npm run build` — correctness was established by manually cross-checking every primitive's actual prop signature (`select.tsx`, `dropdown-menu.tsx`, `dialog.tsx`, `button.tsx`) against how `OrdersPage.tsx` uses them, and by confirming route mounts/response shapes in `server/index.js` and each server route file line-by-line, not by compiling. `node --check` passed clean on `server/routes/orders.js` and `server/scripts/migrate.js`.

### 10. Testimonials made admin-manageable (Done ✅ — 2026-08-12, branch `claude/project-review-changes-yjarew`, not yet merged to `main`, migration not yet run)

The homepage "Voices of our happy customers" section (slider + satisfy-client avatars + rating counter + section background image) and the standalone `testimonials.html` grid were 100% hardcoded (same static 6-person content, "author-1.jpg" through "author-6.jpg", repeated verbatim across both pages). Rebuilt end-to-end following the **exact same pattern as the banners module** (`server/routes/banners.js` + `BannersPage.tsx` + `ImageUpload`), per an explicit request naming that pattern.

- **Database** — new `server/supabase/phase12_testimonials.sql` (registered in `scripts/migrate.js`, after phase11): `testimonials` table (`quote`, `author_name`, `author_role`, `author_image_url`, `rating` numeric default 5, `sort_order`, `is_active`) with the same `"Authenticated users can manage X" ... using (auth.role() = 'authenticated')` RLS policy already used by `marketing_campaigns`/`coupons`/`banners`; adds `settings.testimonial_bg_image_url` (nullable text, same `alter table ... add column if not exists` style as `phase5_maintenance_mode.sql`). Seeds the real 6 testimonials that were hardcoded on the storefront (Harper Anderson/Sophia Carter/Arlene McCoy/Cameron Williamson/Wade Warren/Guy Hawkins, all sharing the one quote that was already identical across every card, rating 4.9, `author-N.jpg`), guarded with `where not exists (select 1 from testimonials)` so re-running the migration never duplicates rows. **Not yet run against the live Supabase instance** — needs `npm run migrate` or the SQL Editor.
- **Backend** — `server/routes/testimonials.js` (new) mirrors `banners.js` field-for-field: `GET /`, `POST /`, `PUT /:id`, `DELETE /:id`, all behind the existing `requireAuth` middleware, mounted at `/api/testimonials` in `index.js` right after banners. In `server/routes/public.js`: new unauthenticated `GET /testimonials` (active only, `order by sort_order`) and a new `GET /settings` (the storefront had no public settings endpoint at all before this — the closest existing thing, `/site-content`, bundles settings *and* banners together and nothing yet consumed it; `GET /settings` is the plain, single-purpose version the new storefront JS actually calls, selecting the same safe column list `/site-content` already used, now including `testimonial_bg_image_url`). `/site-content` itself was also extended with that same new column, for consistency, though nothing currently reads it from there.
- **Admin UI** — `admin/src/pages/TestimonialsPage.tsx` (new): table (image/quote preview/author+role/rating/sort/active badge/edit-toggle-delete actions) + create/edit `Dialog`, copied structurally from `BannersPage.tsx` including its exact Activate/Deactivate button pattern (not a switch component) and its `ImageUpload` usage for the author photo (`folder="ellora/testimonials"`). Added to `Sidebar.tsx` (`Quote` icon from `lucide-react`, right after Banners) and `App.tsx` (`/testimonials` route) the same way Banners is wired in both files.
  - **`testimonial_bg_image_url` lives in `SettingsPage.tsx`**, not a second settings-card on `TestimonialsPage.tsx` — chosen because it's a single sitewide value exactly like `announcement_text`/`footer_copyright_text`, which that page already manages with the same "one form, PUT `/settings` merges whatever keys are present" pattern; added right after those two fields, with an `ImageUpload` in place of the `Input` the other two use.
- **Storefront homepage** (`html/js/homepage-dynamic-sections.js`, new `loadTestimonials()`, section 8 in that file's own header table): fetches `/testimonials` + the new `/settings` together, then (only if at least one active testimonial exists): rebuilds the `.testimonial-slider` swiper slides by cloning the first `swiper-slide` as a template — **real bug caught and fixed during this build**: `js/function.js` already initializes that slider with `loop: true` *before* this script runs (confirmed via the actual `<script>` order in `index.html`), and Swiper's loop mode clones extra slide elements into the wrapper at init time; grabbing "the first `.swiper-slide`" for a template *before* destroying that Swiper instance risked grabbing one of Swiper's own synthetic loop-clones instead of the real authored slide. Fixed by calling `swiperEl.swiper.destroy(true, true)` first (Swiper 6+ exposes the live instance as `element.swiper`, confirmed against the actual bundled `swiper-bundle.min.js`, which is v11.0.5) — destroy removes the loop clones and restores the original static markup — *then* reading the template slide out, exactly the same "destroy the plugin instance before touching its DOM, then rebuild it" approach this same file already uses for the Isotope-powered featured-products grid. Also updates the 3 `.satisfy-client-images` avatars (first 3 testimonials) and the `.counter`/ratings-label text (average of every active testimonial's `rating`, replacing the hardcoded "4.9"), and sets `.our-testimonials`' inline `background-image` from `testimonial_bg_image_url` when set (the `css/custom.css` `.our-testimonials { background-image: url(...) }` rule is untouched, so it's still the fallback when no admin image is set).
- **Storefront testimonials.html** (`html/js/testimonials-page-dynamic.js`, new file, same per-page-script convention as `product-single-dynamic.js`/`blog-single-dynamic.js` — own hardcoded `API_BASE` constant, no shared `get()` helper): fetches `GET /api/public/testimonials`, clones the first `.col-xl-4.col-md-6` testimonial block as a template for every active testimonial (so all 6 rendered blocks are genuinely built from one real template, not five originals mutated in place), hides any static blocks beyond however many testimonials came back, appends extra cloned blocks if more than 6 exist, and cycles the original `0s/0.2s/0.4s/0.6s/0.8s/1s` `wow fadeInUp` stagger via `data-wow-delay` (with `0s` expressed as *no* attribute, matching how the first static block was originally authored). Script tag added to `testimonials.html` right after `wow.min.js`/`function.js`, matching the placement `product-single-dynamic.js` uses on `product-single.html` — `new WOW().init()` is re-run after the DOM rebuild so the cloned blocks still animate in (same "re-init WOW after injecting content" pattern `loadOfferBanners()` already established in `homepage-dynamic-sections.js`).
- **Not verified in this sandbox**: same limitation as every prior admin TS change this session — no `admin/node_modules` here, so `TestimonialsPage.tsx` and the `SettingsPage.tsx` edit were reviewed by hand against `BannersPage.tsx`'s and `SettingsPage.tsx`'s own proven patterns, not compiled. `node --check` passed clean on every changed/new `.js` file, both server-side and the two storefront dynamic scripts.

### 11. Wishlist and Compare wired up on product-single.html (Done ✅ — 2026-08-12, branch `claude/project-review-changes-yjarew`, not yet merged to `main`)

The Wishlist and Compare icon links next to Add To Cart on `product-single.html` were dead `<a href="#">`s. No schema/migration involved this time — Wishlist reuses a real, already-working backend; Compare deliberately gets none.

- **Wishlist reuses the existing authenticated API** — `server/routes/customer.js` (singular; **not** `server/routes/customers.js`, the plural one, which is a different, storefront-profile-flavored router mounted at `/api/customers` that happens to *also* have `/wishlist` sub-routes but isn't what `ElloraAuth`/`account-wishlist.js` actually call — confirmed by reading `ElloraAuth`'s `API_BASE` in `auth.js`, which points at `/api/customer`, singular). `GET /wishlist` / `POST /wishlist` (body `{ product_id }`) / `DELETE /wishlist/:productId` — the same three routes `account-wishlist.js` already uses via `ElloraAuth.apiFetch`. No new endpoint, no migration.
  - **Real UX bug caught before it shipped**: `ElloraAuth.apiFetch()` redirects to `login.html` itself whenever there's no session — correct for the *click* handler (wishlisting requires login) but wrong for the page-load "is this already wishlisted?" check — calling `apiFetch('/wishlist')` unconditionally on every page load would have bounced every anonymous shopper browsing a product page straight to the login screen. Fixed by checking `ElloraAuth.getSession()` (never redirects on its own) first, and only calling `apiFetch()` when a session actually exists.
  - Button state persists via a new `.active` class (`html/css/custom.css`, right after the existing `.product-single-action ul li a:hover` rules — reuses the exact same background/icon-invert treatment those already define, just kept on instead of only on `:hover`). IDs added to the two `<a>` tags (`#product-wishlist-btn`, `#product-compare-btn`) — no other markup restructuring.
- **Compare has no backend, and none was built** — `html/js/ellora-compare.js` (new): a small `window.ElloraCompare` localStorage module at the same tier as `ellora-cart.js` (`get`/`has`/`add`/`remove`/`clear`), capped at 4 items. Adding a 5th doesn't silently evict the oldest — `add()` returns `{ added: false, reason: 'full' }` and the caller toasts "Remove an item to compare more" instead, since silently dropping whatever the shopper compared first seemed more likely to confuse than help. ~~No `compare.html` to actually view the list exists~~ **Built 2026-08-12 — see section 12.**
- **New toast — a real gap, not an existing pattern reused**: the task asked to reuse "whatever toast mechanism `ellora-cart.js` already uses," but `ellora-cart.js` turned out to be pure `localStorage` logic with no UI at all, and the only other candidate (`checkout.html`'s `showMessage()`) is a page-local function tied to a specific inline `<div>` in that one page's own markup, not something importable. Rather than force-fit either, added a minimal `.ellora-toast` component (`css/custom.css`, appended at the very end of the file with its own unnumbered header comment — the file has a numbered index/TOC at the top through section 46, and inserting a proper new numbered section would have meant renumbering everything after it for no real benefit) plus a small `showToast()` helper in `product-single-dynamic.js`, styled with the site's own `--primary-color` so it looks native rather than bolted on. Scoped to this page only for now, same as the Compare button that uses it.
- **Script tags added to `product-single.html`**: `js/ellora-compare.js`, plus the Supabase CDN `<script>` + `js/auth.js` pair every `account-*.html` page already loads (same order) — needed for `ElloraAuth` to exist here at all, since this page never required login-awareness before.
- Wiring lives inside the existing `product-single-dynamic.js` (not a new file) — both buttons need `currentProduct` (id/slug/name/image_url/base_price), which that file already fetches once per page load; a separate file would have meant either a second product fetch or a fragile cross-file read of another script's local variable.

### 12. compare.html built (Done ✅ — 2026-08-12, branch `claude/project-review-changes-yjarew`, not yet merged to `main`)

The follow-up noted in section 11 — a real page to view the `ellora_compare` localStorage list the Compare button (section 11) writes to.

- **`html/compare.html`** (new) — same head/topbar/header/footer/script-include structure as `cart.html`, copied exactly so it matches site styling/navigation rather than drifting. Body is a **column-per-product, row-per-attribute** table (Image/Product/Price/Colour/Size/Category/Add to Cart/Remove) — the transpose of every other list-style page in this codebase (cart/wishlist are row-per-item) — wrapped in Bootstrap's `.table-responsive` for mobile scrolling. There's no compare table anywhere in the original static template to copy from, so the markup/CSS is new, but built from the same pieces everything else here already uses (`.btn-default`, the site's border/spacing rhythm) rather than a new visual language.
- **`html/js/compare-page-dynamic.js`** (new) — reads `ElloraCompare.get()` on load, then re-fetches each item by `slug` from the same `GET /api/public/products/:slug` endpoint `product-single-dynamic.js` already uses, since the lightweight compare-list entries (`id/slug/name/image/price`) don't carry Colour/Size/Category — those come from `product_variants`/`category` on the full product record. Table is always padded to `ElloraCompare.MAX` (4) columns — real products first, then "Add Product" placeholder columns linking to `products.html`. **Handles a real edge case**: if a per-item fetch 404s (the product was deleted or unpublished after being added to compare), that column still renders from the lightweight stored data instead of breaking the whole table — Colour/Size/Category show "—", Add to Cart is disabled ("Unavailable"), and Remove still works, so the shopper can clean up a stale entry rather than being stuck. Remove / Clear / a fresh Add to Cart all just re-run the same `render()` (matching `account-wishlist.js`'s re-render-after-mutate pattern) — no full page reload. **Add to Cart deliberately does not redirect to `cart.html`** the way `product-single-dynamic.js`'s does — the whole point of this page is comparing several products side by side, so navigating away after adding just one would defeat it; it toasts "Added ✓" on the button itself and stays put, reusing `EllroaCart.add()` directly rather than duplicating cart logic.
- **Empty state** follows the same "icon + heading + text + CTA button" shape `account-wishlist.js` already established for its own empty wishlist (that one uses inline `style=` attributes; this one uses a real `.compare-empty-state` CSS class instead, since this was written fresh rather than patched).
- **Nav**: added a `Compare` link to the header's "Pages" dropdown, immediately after `Wishlist`, across **all 28 pages that have that dropdown** — every `html/*.html` page except `reset-password.html`, which has no header/nav chrome at all (a standalone minimal page, confirmed by checking — not an oversight). Applied via a Python regex script matching `.../Wishlist</a></li>` immediately followed by `.../Cart</a></li>` (the nav-dropdown occurrence specifically — every page also has a *second*, unrelated "Wishlist" link in the footer's Customer Services list that must NOT be touched; the regex's "must be immediately followed by the Cart link" requirement is what tells the two apart). `compare.html` itself already has the link baked in from authoring it fresh.
- **Compare button toast now links to the new page** (`product-single-dynamic.js`) — `showToast()` switched from `textContent` to `innerHTML` (still only ever fed hardcoded strings from this file, never user input, so this is safe) so the "Added to compare" / "Removed from compare" / "list is full" toasts can each include a real `<a href="compare.html">View compare</a>`. Required loosening `.ellora-toast`'s CSS too — it was `pointer-events: none` (fine when it was just plain text), so `.ellora-toast.is-visible` now sets `pointer-events: auto` while shown, or the link would render but not actually be clickable.
- **CSS** (`css/custom.css`): new `.compare-table`/`.compare-empty-state`/`.compare-add-placeholder` rules appended at the very end of the file with their own unnumbered header comment (same reasoning as the Toast block above it — the file has a numbered index/TOC through section 46, and a properly numbered new section would mean renumbering everything after it). The "Clear Compare List" button reuses the existing `.cart-item-buttons`/`.btn-clear` classes wholesale (same trash-icon-button treatment "Clear Cart" already has) instead of a parallel new style. **One real bug caught before shipping**: an initial draft used `var(--secondary-color, #b08b5b)` as a link hover color, assuming that CSS variable was an unused/undefined warm accent — it's actually defined in `:root` as `#F3F3F3` (near-white), which would have made hover text on a white page background nearly invisible. Checked the real `:root` block before shipping and replaced it with a plain `opacity: 0.7` hover instead.
- **Not verified in this sandbox**: same limitation as every other storefront change this session — no live server/browser to click through here. `node --check` passed clean on every new/changed `.js` file, and `custom.css`'s brace count was verified balanced (1685/1685) after every edit.
- **Follow-up fix, same day**: the cart-row "Add To Cart" button's text was getting clipped ("Add To" with "Cart" hidden) — not a container-width problem, but `.btn-default`'s `::before` arrow-icon square (40px wide, pinned to `right: 4px` regardless of padding) sitting on top of the text, because `.compare-row-cart .btn-default`'s compact padding (`10px 20px`) only reserved 20px of clearance on the right when the icon needs ~44px. Fixed by scaling the icon down (`::before` to 26px/14px background) to match the compact button and widening the right padding to `34px` to actually clear it, plus `white-space: nowrap`. Styling only, confirmed by reading `.btn-default`'s base rule (`padding: 15px 56px 15px 16px`, `overflow: hidden`) rather than guessing at the cause.

### 13. Support Tickets → seller-ops queue (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, migration not yet run)

`SupportPage.tsx` was a bare table (customer/subject/message/priority/status, no search, no filters, no detail view) — asked to be redesigned to look and work like a real support desk (Myntra/Flipkart-style). Followed the exact pattern the Orders ops console (section 9) already established, rather than inventing a new one.

- **Database** — new `server/supabase/phase13_support_tickets.sql`, registered in `server/scripts/migrate.js` (after phase12) but **not yet run** — needs `npm run migrate` or the SQL Editor: adds `support_tickets.ticket_number` (text, backfilled for existing rows via `row_number() over (order by created_at)` → `TCK-000001` etc., then set `not null` + unique-indexed), `internal_note` (text, agent-only), `resolved_at` (timestamptz, same shape as `orders.shipped_at`/`delivered_at` from phase11).
- **Backend**:
  - `server/routes/public.js` — the contact-form submission endpoint (`POST /api/public/support-tickets`) now generates a real ticket number (`generateTicketNumber()`, same stamp+random shape as `generateOrderNumber()` in `orders.js`, prefixed `TCK-` instead of `ELL-`) and returns it in the response.
  - `server/routes/support.js` — `GET /` now takes `status`/`priority`/`search` query params (search via `.or()` ilike across ticket_number/customer_name/customer_email/subject/message) and returns a `counts` object per status computed off the same filters minus status — same "tab counts respect the other active filters" behavior as Orders' `applyFilters()`. `PUT /:id` now also accepts `internal_note`, and auto-stamps/clears `resolved_at` whenever `status` moves to/from resolved or closed.
- **Admin UI** (`SupportPage.tsx`, full rewrite): stat tiles (Open/In progress/Resolved/Closed counts), status pill tabs + priority filter + debounced search toolbar (mirrors Orders' toolbar), table with customer initials avatar, colored priority/status badges, relative timestamps, open+high-priority rows tinted like Orders' SLA-breached rows; row click (or a row action menu) opens a right-side `Sheet` drawer with the full message, clickable `mailto:`/`tel:` contact links, priority/status editors, and an internal-note textarea with its own Save button; delete via the row menu or the drawer footer.
- **Not yet run against the live Supabase instance** — until phase13 is applied, `ticket_number`/`internal_note`/`resolved_at` don't exist as real columns and `/support` requests will error.
- **Not verified in this sandbox** — no `admin/node_modules`/live server here; correctness confirmed by `npx tsc --noEmit` (clean on `SupportPage.tsx`; one pre-existing unrelated error in `orderPrint.ts`, not touched by this change) and `node --check` on both changed server route files, plus manual cross-check of every UI primitive's real prop signature against how `OrdersPage.tsx` already uses the same ones.

### 14. WhatsApp Auto-Reply Flow — architecture + build (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, migration not yet run, no live WhatsApp credentials yet)

Starting point was a design doc (`WhatsApp_AutoReply_Flow.docx`) spec'ing a 10-option numbered-menu WhatsApp bot for Saint Roman — every branch was static canned text, including "Track My Order"/"Refund Status" which just said "we'll check and update you" (implying a human reads and replies to every message by hand). Per an explicit request, wrote a full architecture plan first (via Claude Code's plan mode — see `/root/.claude/plans/flickering-dazzling-chipmunk.md` for the original approved plan) before writing any code, then implemented Phase A of it end-to-end. Full reasoning on *why* each design choice was made (buttons/lists vs. numbered text, Meta Cloud API direct vs. a BSP, live lookups vs. static replies, ticket hand-off) lives in that plan file — this entry is the "what got built" record.

- **Provider**: Meta WhatsApp Cloud API **direct** (not a BSP like Gupshup/Interakt/Wati) — matches this project's existing build-it-yourself convention (`gemini.js`/`cloudinary.js` call their providers directly, no vendor dashboard layer anywhere else in the codebase). Needs `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_VERIFY_TOKEN`/`WHATSAPP_APP_SECRET` in `server/.env` (placeholders added to `server/.env.example`) — **none of these are configured yet**, so nothing in this section is live/testable until a real (or Meta's free sandbox test) WhatsApp Business number is registered.
- **Database** — new `server/supabase/phase14_whatsapp.sql`, registered in `server/scripts/migrate.js` (after phase13) but **not yet run**: `wa_conversations` (one row per phone number — `current_step`, `context` jsonb, `status` open/handed_off/closed), `wa_messages` (full inbound/outbound log per conversation, used both for debugging and as the ticket transcript). Also extends `support_tickets` (phase13) with `source` (`contact_form`/`whatsapp`, default `contact_form`) and `wa_conversation_id` — reuses the *existing* ticket table for the human-handoff case rather than building a second, parallel ticket system.
- **`server/lib/whatsapp.js`** — thin wrapper over the Graph API's send-message endpoint (`sendText`/`sendButtons`/`sendList`), same shape as `server/lib/gemini.js` (token from env, never reaches the client, throws honestly if unconfigured rather than silently no-op'ing).
- **`server/lib/whatsappFlow.js`** — the entire menu tree as **data**, not a switch-statement (mirrors `NEXT_STATUSES` in `server/routes/orders.js` being a declarative table rather than inline logic): a `menu()`/`info()`/`capture()` builder trio produces every step, so the conversation logic is reviewable without touching webhook plumbing. Every branch the doc left as "we'll check and update you" now hits real data instead:
  - **Track My Order** / **Refund Status** — live queries against `orders`/`returns` (refund lookup resolves `order_number → id` first, then filters `returns.order_id` directly — same pattern `public.js`'s `?category=` slug resolution already uses, since Supabase can't filter a joined table's column without an `!inner` join).
  - **Shop Products** / **Product Info** — real top-level `categories` → real `products` in that category (Shop Products), and a live name search across `products` (Product Info) — not the doc's 2 hardcoded SKUs.
  - **Current Offers / Discount Coupons** — live active rows from `coupons`. **Gift Cards / EMI** honestly reply "not something we offer" instead of fabricating content, since neither feature exists anywhere in this schema (same "make it honest, not fake" call already made for the storefront's Reviews tab in section 8).
  - **Size Guide → "Help Me Choose"**, the 3 "something's wrong" branches under Returns & Exchanges, all 7 branches under Report an Issue, and "Speak to Customer Care" — all create a real `support_tickets` row via the new shared helper (below) instead of dead-ending. Only "Speak to Customer Care" flips the conversation to `handed_off` (bot stops auto-replying, just keeps logging for the transcript) — the other issue-report branches leave the bot active since filing one issue doesn't mean the customer is done with self-service.
  - Product Care and the two policy-text leaves under Returns & Exchanges stay static — there's genuinely no real data source for wash-care copy or return-policy text, so no live query was invented for them.
- **Real UX upgrade over the doc, per an explicit ask**: every menu is a native WhatsApp List Message (≤10 rows) or Reply Buttons (≤3) — the tapped option's `id` drives the flow, not text-parsing "reply with the number," which eliminates the doc's built-in typo/misread risk entirely.
- **`server/lib/supportTickets.js`** (new, shared) — `createTicket()` (moved here from `public.js`, which now imports it instead of duplicating the insert+`generateTicketNumber()` logic) and `createTicketFromConversation()` (resolves the customer's real name/email if `wa_conversations.customer_id` is set, else labels them `WhatsApp (<phone>)`). One function both the contact-form endpoint and every WhatsApp ticket-handoff branch call — no parallel ticket-creation logic anywhere.
- **`server/routes/whatsapp.js`** (new, unauthenticated, mounted `/api/whatsapp` — Meta calls it directly, same tier as `public.js`): `GET /webhook` handles Meta's `hub.challenge` verification handshake; `POST /webhook` verifies `X-Hub-Signature-256` (HMAC-SHA256 over the raw request body, `crypto.timingSafeEqual`) before touching anything — a public unauthenticated endpoint with no signature check would let anyone forge messages into the flow/ticket system. Needed adding a `verify` callback to `express.json()` in `server/index.js` to stash `req.rawBody`, since the signature has to be computed over the exact bytes received, not a re-serialized parse.
- **Admin UI** (`admin/src/pages/SupportPage.tsx`, small addition on top of the section-13 rebuild): a green "WhatsApp" `SourceBadge` on ticket rows and in the detail drawer when `source === 'whatsapp'`; the drawer also fetches and renders a read-only chat-bubble transcript from a new `GET /api/support/:id/transcript` endpoint (`server/routes/support.js`) when a ticket has a `wa_conversation_id`. Deliberately **not** a full new inbox page — the existing Support Tickets queue stays the one ops surface, per the approved plan's "Phase B, not now" call on a dedicated WhatsApp inbox.
- **Explicitly Phase B, not built now** (per the approved plan): WhatsApp's native Catalog integration for richer product browsing, and pre-approved Message Templates for *proactive* order/refund lifecycle pushes ("your order shipped") — WhatsApp only allows free-form replies within a 24-hour window of the customer's last message, so anything proactive needs a Meta-approved template, a separate slower workstream from the fully-reactive flow built here.
- **Known limitation, not addressed**: once a conversation is `handed_off`, nothing currently reactivates it back to `active` (no UI action exists yet) — an agent resolving the ticket doesn't hand the bot back control. Fine for v1 (a human is expected to keep replying via WhatsApp directly at that point), flagged here so it doesn't look like an oversight later.
- **Not verified end-to-end** — no WhatsApp Business credentials exist in this project yet, so the webhook has never actually received a real Meta payload; correctness was established by `node --check` on every new/changed server file (all pass) and by hand-tracing the request shapes against Meta's documented webhook payload format (`entry[].changes[].value.messages[]`, `interactive.list_reply`/`button_reply`), not by an actual test message. **First real verification step**: register a Meta for Developers app, grab the free WhatsApp test number, point its webhook at a local tunnel (ngrok) to this server, and walk all 10 root menu branches from a real phone.

### 15. Inventory page → Myntra/Flipkart-style stock console (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`)

`InventoryPage.tsx` was a flat, unfiltered table (product/variant/SKU/on-hand/reserved/available, one "Adjust" button) — every variant in the catalog on one page, no search, no way to isolate what's actually low or out of stock, and no way to see *why* a stock number changed. Redesigned following the same toolbar+pill-tabs+drawer pattern already established for Orders (section 9) and Support Tickets (section 13), rather than inventing a new layout language.

- **Backend** (`server/routes/inventory.js`, rewritten): `GET /` now takes `search` (product name or SKU), `category`, and `stock_status` query params, and returns a `counts` object (`all`/`in_stock`/`low_stock`/`out_of_stock`) computed off the same search/category filters minus stock_status — same "tab counts respect the other active filters" behavior used by Orders/Support. Each variant is decorated server-side with `available` (`stock_quantity - reserved_quantity`) and a computed `status`: `out_of_stock` when `available <= 0`, `low_stock` when `stock_quantity <= 10` (the **same threshold constant** already used by `dashboard.js`/`notifications.js` for their own low-stock signals, kept in sync by hand), else `in_stock`. The `products!inner(...)` embed is deliberately **unaliased** so `.eq('products.category_id', ...)`/`.or('sku.ilike...,products.name.ilike...')` can filter through it — matches the exact pattern `public.js`'s product-variant color/size filtering already established (`product_variants!inner`, filtered by the real table name) — then renamed back to a clean singular `product` key before the response goes out, so the frontend isn't stuck with a plural key for a single joined row.
- **A real, previously-dead endpoint got wired up**: `GET /:variantId/history` (returns the `inventory_adjustments` audit trail) has existed since the original Module 5 build but was flagged in this file as "not wired to any page yet." Now powers a "View history" action per variant. Also now resolves each adjustment's `created_by` uuid to a real email via `supabaseAdmin.auth.admin.listUsers()` — same approach `team.js` already uses for the Team page — instead of showing a bare uuid.
- **Admin UI** (`InventoryPage.tsx`, full rewrite): stat tiles (Total SKUs, Units on Hand, Low Stock, Out of Stock counts), a search+category toolbar plus stock-status pill tabs with live counts, product thumbnails (reuses `products.image_url`, same field the storefront/Orders already use — no new image field), a combined Available badge (`available · status`, color-coded green/amber/red), row action menu (Adjust stock / View history) replacing the old bare "Adjust" button, out-of-stock rows get the same subtle destructive-tinted background Orders uses for SLA-breached rows, and an Export CSV button (same `csvEscape`/`downloadCsv` pattern already used by `OrdersPage.tsx`, exporting whatever's currently filtered/visible). The Adjust dialog now shows a live "X → Y on hand" preview as the quantity field is typed. History opens in a right-side Sheet (list of adjustments: signed quantity in green/red, reason, note, actor email, relative time) — the first real UI consumer of the history endpoint.
- **Not changed**: multi-warehouse breakdown still doesn't exist (single implicit warehouse, as already noted under Module 5) — "Available" here is just `on_hand - reserved`, not per-location.
- **Not verified in this sandbox** — no `admin/node_modules` live browser here; correctness confirmed by `npx tsc --noEmit` (clean) and `npx oxlint` (clean) on the changed page, `node --check` on the rewritten route file, and by hand-tracing the `products!inner` filter syntax against the exact working precedent in `public.js` rather than assuming it compiles.

### 16. Inventory low-stock automation (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, migration not yet run)

Direct follow-up to section 15's Inventory redesign, picking the top item off that section's own "what's needed to enhance" list: a single global `LOW_STOCK_THRESHOLD = 10` shared by every product regardless of how fast it actually sells, and a purely passive low-stock signal (a badge someone has to notice) with no proactive push anywhere.

- **Database** — new `server/supabase/phase15_inventory_automation.sql` (note: this is the *second* "phase15" in this history — filename collision avoided since the Inventory redesign in section 15 didn't need a migration of its own and so never claimed a phase15 file; this is genuinely phase15), registered in `scripts/migrate.js`, **not yet run**: `product_variants.low_stock_threshold` (nullable int — null falls back to the global default) and `settings.low_stock_alert_phone` (nullable text).
- **`server/lib/stockStatus.js`** (new, shared) — `DEFAULT_LOW_STOCK_THRESHOLD`, `effectiveThreshold()`, `computeStockStatus()`. Before this, the same `LOW_STOCK_THRESHOLD = 10` constant and near-identical status logic were hand-duplicated across `inventory.js`, `dashboard.js`, and `notifications.js` (the inventory.js copy was flagged as "kept in sync by hand" in its own section-15 entry) — now genuinely one definition, and `dashboard.js`'s low-stock widget, the notification bell's low-stock entries, and the Inventory page's status badges are guaranteed to agree, including per-variant threshold overrides. `notifications.js` had to switch from a SQL `.lte('stock_quantity', LOW_STOCK_THRESHOLD)` filter to fetching all variants and filtering in JS, since the threshold is no longer a single constant a `WHERE` clause can express.
- **Real automation, not just a bigger badge**: `POST /inventory/adjust` now compares the variant's stock status before and after the adjustment; if it got worse (in_stock → low_stock → out_of_stock, ranked), it fires a WhatsApp text to `settings.low_stock_alert_phone` — reusing the exact send wrapper built for the Auto-Reply bot (`server/lib/whatsapp.js`, section 14) rather than a second messaging integration. Best-effort and non-blocking (wrapped in try/catch, not awaited into the response): silently no-ops if WhatsApp isn't configured or no alert phone is set, same "works without a key, just doesn't send" pattern the AI Control Center (Module 19) already established — **this alert is architecturally real but will not actually send anything until the WhatsApp credentials from section 14 are configured**, same caveat as that section.
- **New `PUT /inventory/:variantId/threshold`** — sets (or, with an empty body, clears back to default) a per-SKU alert threshold.
- **Admin UI**: `InventoryPage.tsx` gets a "Set alert threshold" row action (small dialog, defaults to the store-wide 10 when empty) and shows "Alert below N" under each row's availability badge. `SettingsPage.tsx` gets a `low_stock_alert_phone` field with an explicit note that it's inert without WhatsApp configured — same honesty standard as everywhere else fake-looking-but-inert config has been flagged in this file.
- **Not verified in this sandbox** — same limitation as every other change this session: `npx tsc --noEmit` and `npx oxlint` clean on both changed admin pages, `node --check` clean on all five changed/new server files, but no live Supabase/WhatsApp credentials to actually trigger and observe an alert end-to-end.

### 17. Fixed the broken admin Customers page — real root cause, not a workaround (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, no migration needed)

The user hit this live: `localhost:5175/customers` showing "Couldn't load customers — connect Supabase and run the Phase 4 schema to enable this." That error message was misleading — the Phase 4 schema (`customers` table) has been fine since Module 7 was first built. The real bug, flagged as a known gap in sections 9 and the old Next Priority #8 but never actually fixed until now:

- **Root cause**: `server/routes/customers.js` (plural, mounted at `/api/customers` in `index.js`) was a **near-duplicate of `server/routes/customer.js`** (singular, mounted at `/api/customer`) — same `requireCustomer`-gated `/profile`/`/password`/`/addresses`/`/orders`/`/wishlist` routes, but with **no bare `GET /`**. `CustomersPage.tsx` has called `GET /api/customers` since it was first built, which 404'd against this file every time. Confirmed via grep that the storefront (`html/js/auth.js`'s `ElloraAuth`, `API_BASE = '/api/customer'`, singular) never once calls the plural path — the plural file's routes were dead code that happened to be squatting on the exact path the admin needed.
- **Fix**: replaced `server/routes/customers.js` entirely with the real admin endpoint — `GET /` (list with live `order_count`/`lifetime_value`), `POST /`, `PUT /:id`, `DELETE /:id`, all behind `requireAuth` (not `requireCustomer` — this is an admin route, not a storefront one). `server/routes/customer.js` (singular) was **not touched** — it's the genuinely working storefront API, confirmed by the same grep.
- **`server/lib/customerStats.js`** (new, shared) — the `order_count`/`lifetime_value`-from-real-orders join used to only exist as a private helper inside `crm.js` (for segment evaluation); now both `customers.js` and `crm.js` import the same function, so the admin Customers list and CRM segment membership can never quietly disagree on a customer's stats.
- **Side effect, also fixed**: the Orders "New order" dialog's "Existing customer" picker (`OrdersPage.tsx`, already calling `GET /customers`) was 404ing for the exact same reason — it now works too, no changes needed on the admin side since it was already calling the right path.
- **Not verified in this sandbox** — `node --check` clean on all three changed/new server files; no live Supabase instance here to actually load the Customers page and confirm the fix end-to-end.

### 18. Real legal policy content on the storefront's 5 policy pages (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, no migration needed)

The user supplied 4 real, India-law-compliant policy documents (Terms and Conditions, Privacy Policy, Return & Exchange Policy, Shipping & Delivery Policy — drafted for **ANNORIA HOUSE OF BRANDS PRIVATE LIMITED**, trading as Saint Roman, under the Consumer Protection Act 2019 + E-Commerce Rules 2020, DPDP Act 2023, IT Act 2000, Legal Metrology Rules 2011). All 5 static policy pages (`html/term-condition.html`, `privacy-policy.html`, `refund-return.html`, `delivery-policy.html`, and `cancellation-policy.html`) had **never been wired to anything real** — every one held leftover generic template placeholder content from the original watches-storefront demo ("we specialize in men's, women's, kids', couple, and smartwatches", `[Currency]`, `[Your Country/State]`, `info@example.com`, "Ellora"), confirmed via grep to be completely untouched by any prior session.

- **Extraction bug caught and fixed along the way**: the first attempt to pull text out of the uploaded `.docx` files (regex over the raw `word/document.xml`, no `pandoc`/`soffice` available in this sandbox for either this or the earlier WhatsApp-flow doc) produced garbled output — raw XML tags like `<w:left w:val="nil"/>` leaking into the "extracted text." Root cause: the text-run regex `<w:t[^>]*>` had no word-boundary after `t`, so it also matched `<w:top>`, `<w:tbl>`, `<w:tcPr>`, etc. (anything starting with `w:t...`), and then swallowed everything up to the next real `</w:t>` — including several paragraphs' worth of structural XML — as if it were paragraph content. Fixed with a boundary-safe pattern (`<w:t(?:\s[^>]*)?>`) that only matches the actual `<w:t>` run-text tag; re-extracted all 4 docs cleanly afterward. Worth remembering if any future doc-reading in this project hits the same "no pandoc/soffice" wall and falls back to hand-rolled XML regex.
- **Structure preserved, content replaced**: all 5 pages already shared one consistent template (sidebar anchor-jump nav + numbered `.privacy-policy-entry-item` sections) — no new CSS/markup pattern was introduced, just the sidebar links and section bodies swapped for the real numbered sections from each document (13 sections for Terms, 10 for Privacy including a real data-category/purpose `<table>`, 8 for Return & Exchange, 10 for Shipping & Delivery including a real region/SLA `<table>` — both tables use Bootstrap's `.table`/`.table-responsive`, already loaded on both pages). `<title>` tags updated from the generic template title to real per-page titles.
- **The docs' own "LEGAL DISCLAIMER" preamble is not rendered** — it's addressed to whoever is publishing the policy ("have your final draft reviewed by an advocate"), not to a customer reading the live page, so it was deliberately left out rather than shown as if it were policy text.
- **Two explicit decisions from the user, not assumed**: (1) the docs' own never-filled-in bracketed placeholders (`[Insert Corporate Office Address]`, `+91-[Insert Customer Support Number]`, `[Insert City, State]` for jurisdiction) are shown **visibly bracketed** on the live pages rather than omitted or invented — so whoever manages the site can see at a glance what real info still needs filling in; (2) `cancellation-policy.html` (a separate page not among the 4 uploaded docs) was **also updated**, reusing the Return & Exchange doc's own short "7. CANCELLATION POLICY" section (Before Dispatch / After Dispatch) — cut down from the old page's 5 fabricated sections to the 2 real ones, with a link through to `refund-return.html` for the full policy.
- **Contact emails used throughout** (`legal@`, `grievance@`, `privacy@`, `support@`, `shipping@saintromanfashion.com`) are taken directly from the documents themselves — not invented — reflecting the department split the docs already define (legal/grievance for Terms, privacy/grievance for the Privacy Policy, support for returns, shipping for delivery).
- **Verification**: for each of the 5 files — `<div>`/`</div>` counts balanced, sidebar `href="#..."` anchors verified to exactly match their target section `id`s (scripted diff, all 5 matched), and a scripted grep over just the content region (page-header through Main-Footer-Start) confirmed zero remaining instances of `info@example.com`, "Ellora", "Lorem", `[X]`, or `[Currency]` — i.e., no leftover placeholder content slipped through. Not verified: actual rendering in a browser (no live server in this sandbox) — the anchor-jump JS and `wow`/wow.js wow fadeInUp animation classes were left untouched, so behavior should match the original template exactly, just with real text.
- **Deliberately out of scope**: sitewide branding (header logo, nav, footer copyright still say "Ellora" elsewhere on the site) — only these 5 policy pages' own content was touched, per the user's request.
- **Follow-up fix, same day**: `refund-return.html`'s `<h1>` still read "Cancellation, Return & Exchange Policy" after the rewrite above, even though the page's own `<title>` and breadcrumb already just said "Return & Exchange Policy" — caught live by the user looking at the rendered page. Trimmed to match.

### 19. New Corporate & Compliance page (Done ✅ — 2026-08-14, branch `claude/support-ticket-design-q3s0xb`, not yet merged to `main`, no migration needed)

The user supplied a real "Corporate & Compliance" document (company registration, legal metrology compliance, statutory grievance redressal, IP/trademark declaration, regulatory nodal contact — for **Saint Roman Private Limited** / Annoria House of Brands Private Limited) and asked for a brand-new page, not an edit to an existing one. A second doc, "About Us / Company," was also uploaded in the same message — the user explicitly confirmed via `AskUserQuestion` to leave `about.html` untouched for now and build only the Corporate & Compliance page.

- **New `html/corporate-compliance.html`** — built from the same proven template all 5 policy pages already share (sidebar anchor-jump nav + numbered `.privacy-policy-entry-item` sections, `<title>Corporate & Compliance | Saint Roman</title>`), rather than inventing new markup. 5 sections: Corporate Identification & Registered Office (real CIN `U14109KA2026PTC225933`, real PAN `ABGCA4379N`, real registered office address in Bangalore — all taken verbatim from the doc, not invented; GSTIN and corporate HQ address stay visibly bracketed since the doc never filled them in either, same "keep placeholders visible" standard as section 18), Legal Metrology & Brand Compliance, Statutory Grievance Redressal Mechanism (officer contact + 48h/30-day SLAs), Intellectual Property & Trademark Declaration, and Regulatory Nodal Contact for Authorities.
- **Sitewide footer link rollout** — added `<li><a href="corporate-compliance.html">Corporate & Compliance</a></li>` right after the existing "Terms & Conditions" link in the footer's "Customer Services" list, across **30 other HTML files** (every page carrying that footer block) via a scripted find-and-replace rather than 30 manual edits — same approach already used for the sitewide "Compare" nav-link rollout in section 12. Confirmed the new page is now reachable from every page's footer, not just by direct URL.
- **Verification**: same battery as section 18 — `<div>` balance (80/80), sidebar anchor `href="#corp_N"` vs section `id="corp_N"` diff (all 5 matched), and a content-region grep for leftover placeholder text (clean). Also spot-checked that the scripted footer-link insertion didn't touch `<div>` counts on a few of the 30 edited files (confirmed via `git diff` — each edit was a single added `<li>` line). **Noted, not caused by this change**: `products.html` has a pre-existing 87-vs-86 `<div>`/`</div>` count mismatch unrelated to this edit (confirmed via diff that this session's change to that file was only the one added footer line) — flagging so it doesn't look like something introduced here if it ever needs fixing.
- **Not verified in this sandbox**: no live browser to click through the new page or confirm the footer link renders correctly on all 30 pages.

---

## 🔨 BUILD PLAN — 8 ESSENTIAL MODULES (First Release)

### Phase 1: Foundation
- [x] Set up Supabase project — **DONE.** Project `gxbebydzhrmjvnkyryub` (region `ap-south-1`) created, `server/.env` + `admin/.env` filled with real keys, schema deployed (see "Verified working end-to-end" note below).
- [x] Create Express API server with Supabase client (`server/index.js`, `config/supabase.js`, `middleware/auth.js`, routes for auth + settings)
- [x] Set up React + Vite + Tailwind + Shadcn/ui for admin frontend (`admin/`)
- [x] **Module 22: Auth & User Management (RBAC now real)** — Login page, session via Supabase Auth, `AuthContext`, `ProtectedRoute`. **RBAC was silently broken until 2026-07-21**: `requireRole()` checked `user_metadata.role`, which nothing ever set — every check defaulted to `'admin'` and passed regardless of real role. Fixed to read the real `profiles.role`. New `TeamPage.tsx`/`server/routes/team.js` (admin-only): list team members, create new ones (no email-invite system — generates and displays a one-time temp password), change roles. Applied `requireRole('admin')` to `PUT /api/settings` and `GET /api/audit` as proof points (most other routes still have no role restriction). **Tested live, the real proof**: created a `manager`-role user, logged in as them, confirmed `GET /api/audit` → 403 and `PUT /api/settings` → 403 while `GET /api/settings` stayed 200. No 2FA/SSO (no external provider).
- [x] **Module 23: Settings (extended)** — site title, currency, tax; added announcement bar text, footer copyright text, and **Maintenance Mode** (`server/supabase/phase5_maintenance_mode.sql`). Maintenance Mode wired into `products.html` (whole-page replacement with a "We will be back soon" message when enabled). **Tested live, on and off**: enabled it, confirmed the storefront blocked; disabled it, confirmed normal operation returned. Not built: Payment Gateway/Email/SMS/WhatsApp-API-key/CDN/Image-Compression/Backup config — storing settings for providers that don't exist would be inert fields.

### Phase 2: Core Commerce
- [x] **Module 4: Category Management** — `categories` table (unlimited depth via `parent_id`), CRUD API (`server/routes/categories.js`), admin page with list + create dialog (`admin/src/pages/CategoriesPage.tsx`). **Tested live**: created a real "Men's Fashion" category. Sub-category UI (nested tree), collections, and an edit button are not yet built — first pass is flat list, create/delete only.
- [x] **Module 2: Product Management** — `products` table (core fields: SKU, barcode, brand, HSN, GST%, category, price, status, SEO title/description — not the full 27+ field spec yet), CRUD API (`server/routes/products.js`), admin page (`ProductsPage.tsx`) with **create, edit, and delete** all wired and tested live (created "Classic Denim Jacket," edited its price 2499 → 1999, later added Brand/Barcode/HSN/GST fields to the form and verified they persist). Delete now has a confirmation prompt. Image upload (Media Matrix / Cloudinary) and the AI description optimizer are still not built — deliberately deferred until there's Supabase Storage/Cloudinary and an LLM integration to back them.
- [x] **Module 3: Variant Management** — `product_variants` table (size/color/pack/price/stock), nested create/update in the products API and inline variant rows in the product form (variants can be added but not individually removed from the form). Per-variant images not yet wired.

**Verified working end-to-end (2026-07-21):** the Supabase connection was live but the schema had never actually been run — all tables came back "not found." Fixed by running `server/supabase/schema.sql` + `phase2_catalog.sql` via a new script, `server/scripts/migrate.js` (uses the `pg` package, connects via `DATABASE_URL` in `server/.env` — the **session pooler** string, `aws-1-ap-south-1.pooler.supabase.com:5432`, not the direct `db.<ref>.supabase.co` host, which only resolves over IPv6 and isn't reachable from this network). Test login: `admin@ellora.test` / `EllroaAdmin@2026`. Both servers run via `npm run dev:server` (port 4000) and `npm run dev:admin` (port 5173).

### Phase 3: Operations
- [x] **Module 5: Inventory Management (upgraded 2026-08-14 — see sections 15-16)** — added `reserved_quantity` to `product_variants` + a new `inventory_adjustments` audit-log table (`server/supabase/phase3_inventory.sql`), API (`server/routes/inventory.js`: `GET /`, `POST /adjust`, `GET /:variantId/history`), admin page (`admin/src/pages/InventoryPage.tsx`) listing every variant with on-hand/reserved/available stock and an "Adjust" dialog (quantity delta + reason: purchase order / manual adjustment / cycle count / return / damage + optional note). **Tested live** (original build): added a size/color variant to the test product, adjusted its stock +25 through the UI, confirmed the number updated, persisted after a fresh page reload, and the audit row landed in `inventory_adjustments` with the correct `created_by`. Redesigned 2026-08-14 into a Myntra/Flipkart-style stock console — search/category/stock-status filters with live counts, and the previously-unwired `GET /:variantId/history` endpoint now powers a real adjustment-history drawer (section 15); then given a per-SKU alert threshold and a real WhatsApp push when a variant crosses into low/out-of-stock (section 16, **not yet live** — needs both phase15 migrations run and WhatsApp configured). Still not built: multi-warehouse breakdown (single implicit warehouse), Purchase Order entities as their own trackable object (POs are currently just an adjustment reason, not a full PO workflow), stock transfer between warehouses, cycle-count sessions.
- [x] **Module 6: Order Management (first pass)** — new `orders`/`order_items` tables with the full 10-status `order_status` enum from the spec (`server/supabase/phase3_orders.sql`), API (`server/routes/orders.js`: `GET/POST /`, `GET /:id`, `PUT /:id` for status/tracking), admin page (`OrdersPage.tsx`) — create-order dialog picks line items from the Inventory variant list and computes totals, inline per-row status-change dropdown. Creating an order decrements the linked variant's stock. **Tested live end-to-end**: created an order for "Rahul Verma" (1× Classic Denim Jacket M/Blue + ₹99 shipping), confirmed total = ₹2098, confirmed the variant's stock dropped 75→74 in Inventory, changed status pending→processing via the dropdown and saw it persist. Not yet built: Module 7 Customer Management (customer name/email/phone live directly on the order row for now, no `customers` table or order history per customer), invoice generation, real shipment/tracking integration (just a plain text `tracking_number`/`courier` field), partial shipment/cancellation, fraud score, gift wrap, order notes UI (the column exists, no UI yet). Also: no public storefront checkout exists, so orders are admin-entered only; the stock-decrement-per-line-item loop on order creation isn't wrapped in a DB transaction, so a mid-loop failure could leave stock partially decremented — fine for single-admin testing, worth revisiting before multi-user use.
- [x] **Module 17: CMS** — Blog Posts + FAQs (`server/supabase/phase3_cms.sql`, `server/routes/cms.js`, `BlogPostsPage.tsx`/`FaqsPage.tsx`), plus later Announcement Bar + Banners + Footer (`server/supabase/phase5_cms_content.sql`, new `banners` table, `BannersPage.tsx`, `settings.announcement_text`/`footer_copyright_text`). **Tested live**: FAQ/blog post creation with auto-slug; announcement bar and footer copyright both confirmed showing live admin-set text on `products.html`; banner CRUD confirmed round-tripping through the public API. **(Update 2026-07-27: banners now have a display surface** — a hero strip on `products.html` renders active banners with images; see the Image uploads / Media Library note near the end.) Still not built: Homepage Builder, Landing Pages, Popup Management, Mega Menu — all need either a real page-builder UI or a display mechanism this static template lacks.

---

## 📋 FULL 25-MODULE VISION (Future Phases)

### Phase 4: Customer & Finance (After first release)
- [x] **Module 7: Customer Management (first pass; regressed, then fixed 2026-08-14 — see section 17)** — new `customers` table + `orders.customer_id` FK (`server/supabase/phase4_customers.sql`), API `server/routes/customers.js` (list endpoint computes `order_count`/`lifetime_value` by joining orders), `CustomersPage.tsx`, and an "Existing customer" picker added to the Orders "New order" dialog (auto-fills name/email/phone, links `customer_id`). **Tested live end-to-end at the time**: created customer "Priya Sharma," placed an order for her via the picker, confirmed her record updated to `order_count: 1` / `lifetime_value: 1999`. **Later regressed** — at some point `server/routes/customers.js` (this file) got overwritten with a near-duplicate of the storefront's `customer.js` (singular), losing its bare `GET /`/`POST /` and silently 404ing both `CustomersPage.tsx` and the Orders customer picker for an unknown stretch of time until a user hit it live and it was fixed (section 17). Not built: wishlist, wallet, reward points, buying-frequency/preferred-category analytics — deferred until there's more real purchase history. No Supabase Auth link (still admin-entered only, no public signup).
- [x] **Module 10: Coupon Engine (first pass)** — unblocked once real checkout existed. `coupons` table (percentage/flat/free_shipping) + `orders.coupon_code`/`discount_amount` (`server/supabase/phase5_coupons.sql`). Shared `server/lib/coupons.js` validation used by both a live-preview endpoint and order creation (always re-validated server-side, never trusts a client-supplied discount). `CouponsPage.tsx`, real coupon box on `checkout.html`. **Tested live**: applied SAVE10 (10%) to a ₹1999 item, confirmed -₹199.90 discount and ₹1799.10 total, confirmed `usage_count` incremented after the order placed. Not built: BOGO, Buy X Get Y, referral/employee coupon types.
- [x] **Module 14: Returns & Refunds (first pass)** — `returns` table with a `return_status` enum, tied to a specific `order_item` (`server/supabase/phase4_returns.sql`), API `server/routes/returns.js` (create computes refund amount; marking "refunded" **automatically restocks the variant** and logs it in `inventory_adjustments`), `ReturnsPage.tsx` (order → item picker, inline status dropdown). **Tested live end-to-end**: created a return, marked it refunded, confirmed Inventory stock incremented automatically. Not built: approval workflow gating (any status can be set at any time), exchange-to-different-item flow, return shipping/pickup logistics.
- [x] **Module 15: Finance Dashboard (first pass)** — no new tables, pure aggregation off `orders`+`returns` (`GET /api/finance/summary` in `server/routes/finance.js`), `FinancePage.tsx` with stat cards (revenue, refunds, net revenue, avg order value, pending returns) + orders-by-status breakdown. **Tested live and hand-verified every number** against known order/return data — all correct. Deliberately did not build Invoices, Credit/Debit Notes, Payouts, Vendor Payments, or GST reporting — none have a real data source yet (no vendors, no payout runs, tax isn't captured per-order-line at sale time), so building UI for them now would be speculative.

### Phase 5: Growth & Intelligence
- [x] **Module 1: Executive Dashboard (first pass)** — no new tables, pure aggregation over `orders`/`customers`/`products`/`product_variants`/`returns` (`GET /api/dashboard/summary` in `server/routes/dashboard.js`). Replaced the literal `DashboardPage.tsx` placeholder (four cards showing "—") with real stat cards (orders, revenue, customers, active products, pending returns) + a recent-orders table + a low-stock table (≤10 units) + an orders-by-status badge row + a live notification bell (Module 21, see below). **Tested live, every number hand-verified**: Orders 5, Revenue ₹8,794 (checked by hand against the 5 known order totals), Customers 4, Active Products 3 — all correct. No drag-and-drop widgets (Module 25) — fixed layout, not the customizable grid from the full spec; not built at all.
- [x] **Module 8: CRM (first pass)** — `customer_segments` table (rule-based: `min_orders` + `min_spend`) (`server/supabase/phase6_crm.sql`, `server/routes/crm.js`, `CrmPage.tsx`). Segment membership is **evaluated live** against real customers each request — `order_count`/`lifetime_value` computed from actual orders (same join the Customers module uses), never stored stale. **Tested live**: created a segment, confirmed it evaluated across the 6 real customers with correct member counts, expandable member list in the UI. Not built: email/SMS campaign sending (no ESP/SMS provider), ML/behavioural segmentation.
- [x] **Module 9: Marketing Dashboard (first pass)** — `marketing_campaigns` table (name/channel/spend/coupon_code) (`server/supabase/phase6_marketing.sql`, `server/routes/marketing.js`, `MarketingPage.tsx`). Reuses the **coupon-attribution** approach from the Influencer module: attributed revenue/orders/ROAS are computed server-side from real orders that used the campaign's coupon — not typed-in performance numbers. Summary stat cards (total spend, attributed revenue, overall ROAS). **Tested live** end-to-end (create → attribution read-back → delete). Not built: live Google/Meta/TikTok ad-account API sync (needs real ad accounts + OAuth).
- [x] **Module 16: Reports & Analytics (first pass)** — added `products.cost_price` (admin-only) to enable a real Profit Report. Single generic `GET /api/reports/:type` (`server/routes/reports.js`) supporting Sales/Customer/Inventory/Product Performance/Returns/Profit, `ReportsPage.tsx` renders any of them generically + client-side CSV export. **Tested live** across multiple report types, all correct. Not built: Marketing/Warehouse/Employee reports (no data source), true Excel/PDF generation or Schedule Reports (no doc-gen lib or job scheduler — CSV substitutes). **Found and fixed a real bug while testing this**: `PUT /api/products/:id` was deleting and recreating all variants (fresh UUIDs) on every edit, which via `ON DELETE SET NULL` silently orphaned `order_items.variant_id` on every historical order each time a product was edited. Fixed to upsert variants by `(size, color)` instead — verified a variant's `id` now survives an edit.

### Phase 6: Advanced
- [x] **Module 11: Influencer Dashboard (first pass)** — ties to the Coupon Engine rather than a separate discount system: `influencers.coupon_code` FKs `coupons.code`. Sales/commission computed server-side from real `orders.total` where the coupon was used — not manually entered. `server/routes/influencers.js`, `InfluencersPage.tsx`. **Tested live**: influencer tied to SAVE10 correctly showed ₹1799.1 sales / ₹179.91 commission (10%), matching the one real order that used it. No follower/engagement/content tracking (no social API).
- [x] **Module 12: Warehouse Dashboard (first pass)** — no new backend at all; `WarehousePage.tsx` is a 3-column Kanban (Picking/QC/Ready to ship) filtered from the existing Orders API, "advance stage" buttons call the existing order-status endpoint. **Tested live**: an order moved through stages, board updated in real time.
- [x] **Module 13: Shipping (first pass)** — `shipping_rates` config table (zone / min-order / rate / free-above) (`server/supabase/phase6_shipping.sql`, `server/routes/shipping.js`, `ShippingPage.tsx`). Plus a **Shipments view derived from real orders** (`GET /shipping/shipments` filters orders in fulfilment statuses); courier + tracking number are edited inline and written straight back onto the order row. **Tested live**: rate-rule CRUD + shipments list returned a real order. Not built: courier API rate-shopping / label generation / webhook tracking (needs a Shiprocket/Delhivery-style integration) — courier/tracking stay plain fields.
- [ ] Module 18: Mobile App Management — deferred, no mobile app exists to manage
- [x] **Module 19: AI Control Center (first pass — Google Gemini)** — `server/lib/gemini.js` (thin wrapper over Gemini's `generateContent` REST API; key + model from `GEMINI_API_KEY`/`GEMINI_MODEL` in `server/.env`, default model `gemini-2.0-flash`, key never leaves the server), `server/routes/ai.js` (`GET /status`, `POST /generate-description`, `POST /generate-seo`), `AiPage.tsx` (product-detail form → generated description + SEO title/meta, each copyable; shows a "add your key" card when unconfigured). **Tested live without a key**: `/ai/status` → `{configured:false, model:'gemini-2.0-flash'}`, and generation returns a clean 502 "GEMINI_API_KEY is not set" instead of crashing. Full generation works as soon as a real key is added to `server/.env` (the user chose Gemini over Anthropic/OpenAI/Ollama). Not built: bulk generation, image generation, chat assistant, per-call cost tracking.
- [x] **Module 20: Customer Support (upgraded 2026-08-14 — see sections 13-14)** — `support_tickets` table, and a public ticket-creation endpoint wired to `html/contact.html`'s previously-dead contact form. **Tested live end-to-end** (original build): submitted the real contact form, ticket appeared in admin, changed its status, confirmed it persisted. Admin UI (`SupportPage.tsx`) was redesigned 2026-08-14 into a Myntra/Flipkart-style ticket queue — ticket numbers, status/priority tabs+filters, search, a detail drawer, and internal agent notes; see section 13 for the full breakdown (**migration not yet run**). **WhatsApp is no longer "no provider"** — a full Meta Cloud API bot (webhook, live order/refund/product lookups, ticket hand-off) was architected and built the same day; see section 14 (**not yet live — no WhatsApp credentials configured, migration not yet run**). Still no live phone/calls integration.
- [x] **Module 21: Notifications (first pass — "Internal Alerts" only)** — no persisted table; `GET /api/notifications` derives a live feed each request from recent orders/low-stock/pending-returns/open-tickets (same approach as Dashboard/Reports). Bell icon + count badge added to the admin `Header.tsx`. **Tested live**: badge showed 5, panel listed the 5 real recent orders. No email/SMS/WhatsApp/Push/Slack (no provider).
- [x] **Module 24: Audit Logs (first pass)** — new `audit_logs` table + `server/lib/audit.js` `logAudit()` helper. Instrumented the highest-value mutation points only (not a blanket "log every API call"): admin logins, product price changes, product deletions, order status changes, refunds. **Found along the way**: `POST /api/auth/login` is dead code (frontend signs in via the Supabase SDK directly) — added `POST /api/auth/log-login`, called by `AuthContext.tsx` right after a successful client-side sign-in, as the only way to observe logins server-side. **Tested live**: signed out/in → login logged; changed a price → price_change logged with exact before/after; advanced an order → status_change logged correctly.
- [x] **Module 25: Drag-and-Drop Dashboard Widgets** — `DashboardPage.tsx` refactored into a widget registry (stats / recent orders / low stock / orders-by-status). A "Customize" toggle reveals drag handles (native HTML5 DnD, no new dependency) + per-widget show/hide; order + hidden set persist to `localStorage` (`ellora.dashboard.order` / `ellora.dashboard.hidden`), with a "Reset layout" button. All numbers still come from the live `/dashboard/summary`.

---

## 📁 TARGET FILE STRUCTURE (Revised)

```
project-root/
├── html/                          # Storefront development code (SRC)
│   ├── account-address.html
│   ├── account-addresses.html
│   ├── account-dashboard.html
│   ├── account-details.html
│   ├── account-download.html
│   ├── account-order-details.html
│   ├── account-order.html
│   ├── account-wishlist.html
│   ├── forgot-password.html
│   ├── reset-password.html
│   ├── index.html
│   ├── products.html
│   └── js/
│       ├── auth.js                # Supabase Auth Client wrapper
│       ├── account-dashboard.js
│       ├── account-details.js
│       ├── account-wishlist.js
│       ├── account-addresses.js
│       ├── account-orders.js
│       ├── forgot-password.js
│       ├── reset-password.js
│       └── mega-menu-dynamic.js
├── dist/                          # Compiled storefront output (minified HTML, extracted minified JS)
├── scripts/
│   └── build-html.js              # Production build compiler for storefront
├── server/                        # Express API backend
│   ├── index.js                   # Express entry point
│   ├── config/
│   │   └── supabase.js            # Supabase client init
│   ├── middleware/
│   │   └── auth.js                # RBAC security middleware
│   ├── lib/
│   │   ├── audit.js               # Audit logger helper
│   │   ├── barcode.js             # Code128 generation logic
│   │   ├── cloudinary.js          # Cloudinary media connector
│   │   ├── coupons.js             # Discount validations
│   │   └── gemini.js              # Google Gemini wrapper
│   ├── routes/
│   │   ├── ai.js                  # Gemini details/SEO suggestions
│   │   ├── audit.js               # Admin audit log routes
│   │   ├── auth.js                # Core auth logic
│   │   ├── banners.js             # Layout banner adjustments
│   │   ├── categories.js          # Catalog classification APIs
│   │   ├── cms.js                 # Blog, FAQ, Page resources
│   │   ├── coupons.js             # Promotion codes management
│   │   ├── crm.js                 # Customer segmentation rules
│   │   ├── customer.js            # Storefront secure customer dashboard APIs
│   │   ├── customers.js           # Admin customer records list
│   │   ├── dashboard.js           # Aggregated statistics
│   │   ├── finance.js             # Transaction totals & analytics
│   │   ├── influencers.js         # Affiliate coupons and payments
│   │   ├── inventory.js           # Stocks adjustments & history
│   │   ├── marketing.js           # Ad campaign attribution rates
│   │   ├── media.js               # Cloudinary library asset index
│   │   ├── notifications.js       # Auto-derived operational triggers
│   │   ├── orders.js              # Transaction lifecycle management
│   │   ├── products.js            # Base catalog CRUD & variants upsert
│   │   ├── public.js              # Public site content (products/banners)
│   │   ├── reports.js             # Performance report exports
│   │   ├── returns.js             # Returns & auto-restocking logic
│   │   ├── settings.js            # Global configuration details
│   │   ├── shipping.js            # Rate zones definition
│   │   ├── support.js             # User ticketing logs
│   │   ├── tags.js                # Meta tag queries
│   │   ├── team.js                # Role configurations
│   │   └── upload.js              # Media base64 upload pipeline
│   ├── scripts/
│   │   ├── backfill-barcodes.js   # Barcode population script
│   │   └── migrate.js             # PostgreSQL migrate executor
│   ├── supabase/                  # Database migration schemas
│   └── .env
├── admin/                         # React admin frontend (Vite)
│   ├── src/
│   │   ├── components/
│   │   │   ├── ui/                # Shadcn primitives
│   │   │   └── shared/
│   │   │       ├── BarcodeSvg.tsx # Inline SVG barcode renderer
│   │   │       ├── Sidebar.tsx
│   │   │       ├── Header.tsx
│   │   │       └── ImageUpload.tsx
│   │   ├── pages/                 # UI pages for all 25 modules
│   │   │   ├── AiPage.tsx
│   │   │   ├── AuditLogsPage.tsx
│   │   │   ├── BannersPage.tsx
│   │   │   ├── BarcodeScannerPage.tsx
│   │   │   ├── BlogPostsPage.tsx
│   │   │   ├── CategoriesPage.tsx
│   │   │   ├── CouponsPage.tsx
│   │   │   ├── CrmPage.tsx
│   │   │   ├── CustomersPage.tsx
│   │   │   ├── DashboardPage.tsx
│   │   │   ├── FaqsPage.tsx
│   │   │   ├── FinancePage.tsx
│   │   │   ├── InfluencersPage.tsx
│   │   │   ├── InventoryPage.tsx
│   │   │   ├── LoginPage.tsx
│   │   │   ├── MarketingPage.tsx
│   │   │   ├── MediaPage.tsx
│   │   │   ├── OrdersPage.tsx
│   │   │   ├── ProductsPage.tsx
│   │   │   ├── ReportsPage.tsx
│   │   │   ├── ReturnsPage.tsx
│   │   │   ├── SettingsPage.tsx
│   │   │   ├── ShippingPage.tsx
│   │   │   ├── SupportPage.tsx
│   │   │   ├── TeamPage.tsx
│   │   │   └── WarehousePage.tsx
│   │   ├── context/
│   │   │   └── AuthContext.tsx
│   │   ├── lib/
│   │   │   ├── barcodePrint.ts    # Print layout configuration for labels
│   │   │   └── supabaseClient.ts
│   │   └── App.tsx
│   └── package.json
└── package.json
```

---

## 🗄️ DATABASE (Supabase PostgreSQL)

Key tables currently deployed:
1. `profiles` — Admin users with roles (admin, manager, warehouse, marketing, finance, support, vendor).
2. `settings` — Global configuration, announcement parameters, and maintenance mode status. Plus (phase15, **not yet applied**): `low_stock_alert_phone`.
3. `categories` — Parent-child hierarchical database tags.
4. `products` — Base catalog descriptions including SKU, barcode, cost price, base price, brand, HSN, GST%, `image_url` (the real single product image, added `phase6_banner_image.sql`), and `specifications` jsonb (added `phase8_product_specifications.sql` — label/value pairs for the storefront's Additional Information tab, e.g. `{"Material": "Cotton", "Fit": "Regular Fit"}`).
5. `product_variants` — Product attribute sets containing specific size, color, stock, and pricing increments. Plus (phase15, **not yet applied**): `low_stock_threshold` (nullable int, per-SKU override of the store-wide default — see section 16).
6. `product_images` — **Legacy/unused.** Nothing in the codebase (admin or server) ever inserts into this table — the admin's image upload writes straight to `products.image_url` instead. `GET /api/public/products/:slug` still selects it (harmless, always empty) but don't build anything new against it; use `products.image_url`.
7. `inventory_adjustments` — Variant adjustments log storing reasons (damages, count, purchase, returns).
8. `orders` — Store customer transactions mapped through statuses (pending, processing, shipping, completed, refunded).
9. `order_items` — Line items referencing individual variants.
10. `cms_pages` / `blog_posts` / `faqs` — Customer support, general static and dynamic reading contents.
11. `banners` — Store banner layout descriptors.
12. `media_assets` — Uploaded storage files cache mapped to Cloudinary paths.
13. `customers` — Storefront account profiles (first_name, last_name, display_name, email, phone).
14. `customer_addresses` — Address books with type classification (billing/shipping).
15. `customer_wishlist` — Saved custom catalogs per shopper.
16. `customer_segments` — Analytical filter blocks grouping matching customers.
17. `marketing_campaigns` — Cost budgets and conversion data points linked to code inputs.
18. `shipping_rates` — Logistics rules grouped by target zone and cart requirements.
19. `support_tickets` — Inquiries coming from web contact forms. Plus (phase13, **not yet applied**): `ticket_number` (human-readable, e.g. `TCK-000001`), `internal_note` (agent-only), `resolved_at`. Plus (phase14, **not yet applied**): `source` (`contact_form`/`whatsapp`), `wa_conversation_id`.
20. `audit_logs` — Traceable system events (admin logins, price alterations, deletions, refunds).
21. `wa_conversations` / `wa_messages` (phase14, **not yet applied**) — one row per WhatsApp phone number tracking the bot's current menu step + jsonb context, and the full inbound/outbound message log for that conversation (doubles as the transcript attached to a support ticket on human hand-off).

---

## 🔐 AUTH & ROLES

| Role | Access |
|------|--------|
| admin | Everything |
| manager | Products, orders, customers, reports |
| warehouse | Inventory, warehouse, shipping |
| marketing | CRM, coupons, marketing, influencers |
| finance | Finance, reports, invoices |
| support | Customer support, returns |
| vendor | Own products, own orders |

**First release**: Single admin role only. Multi-role RBAC added later.

---

## 📝 IMPORTANT NOTES

1. ~~The `html/` folder is the ORIGINAL template — never modify it, keep as reference~~ **Stale — this stopped being true a while ago and nobody had corrected it.** `html/` is the actively-developed storefront source (served by `npm run dev` / browser-sync at `:3000`); most pages are wired to live data via inline `<script>` blocks and/or dedicated `html/js/*-dynamic.js` files (`mega-menu-dynamic.js`, `homepage-dynamic-sections.js`, `blog-single-dynamic.js`, `product-single-dynamic.js`, `products-hero-dynamic.js`, `currency.js`, `ellora-cart.js`, `auth.js`, `account-*.js`, ...). `dist/` is the separate compiled/minified build output (via `scripts/build-html.js`) — that's the one to leave alone and regenerate, not hand-edit.
2. Supabase project is created and connected (see NEXT ACTION) — no need to create another
3. User needs a **Cloudinary account** at https://cloudinary.com (free tier) — still not set up, needed once product image upload is built
4. Reference docs are in project root: `Building a production Admin Panel.docx` (the real phased build spec) and `Required key components in Admin panel.docx` (flat 25-module feature checklist)
5. **Ignore** `Ellora_Backend_Architecture_Report.docx`/`.pdf` (there's a `.pdf` copy directly in `C:\Users\DELL-G3 15-3500\Downloads\`, outside this project folder) — it documents a completely different, abandoned Express+EJS+SQLite architecture from before the Supabase/React pivot. Confirmed stale 2026-07-21.
6. Admin panel should feel premium — dark sidebar, clean forms, Shadcn/ui components
7. **Git**: project now has its own repo, scoped to this folder — pushed to `https://github.com/Puneethgowda4653/Saint-Roman`. Still avoid `git add -A`/`git add .` from the home directory (`C:\Users\DELL-G3 15-3500`) — that separate, unrelated repo still exists at home and is a different concern.
8. **Active branches (as of 2026-08-14)**: two unmerged feature branches, don't assume either is live locally without checking. `claude/support-ticket-design-q3s0xb` is `claude/project-review-changes-yjarew` **plus** one commit — it was branched from `yjarew`, not from `main` (verified via `git log claude/support-ticket-design-q3s0xb ^claude/project-review-changes-yjarew` → exactly 1 commit ahead, 0 behind), so it already contains everything from sections 8-12 as well as section 13. Neither branch is merged into `main` on GitHub.
   - `claude/project-review-changes-yjarew` — sections 8-12 (Downloads sidebar, blog wiring, nav reorder, product-single.html fixes, product specifications, checkout bug chain, the Orders ops console redesign, admin-managed testimonials, the Wishlist/Compare buttons, and compare.html). `git checkout claude/project-review-changes-yjarew && git pull` to get it.
   - `claude/support-ticket-design-q3s0xb` — sections 8-12 **and** section 13 (Support Tickets redesign). `git checkout claude/support-ticket-design-q3s0xb && git pull` to get it — this is the more complete branch of the two.
   - There is also a third branch on origin, `feat/phase6-modules`, not touched or investigated in any session documented here — check it before assuming it's unrelated/stale.

---

## 🚀 NEXT ACTION

**As of August 14, 2026**: Blog is now fully wired (admin → API → storefront, homepage + blog.html + blog-single.html), the storefront header nav was reordered (now including a Compare link on every page), `product-single.html` went from mostly-static-demo-content to fully live (including now-functional Wishlist/Compare buttons, and a new `compare.html` page to actually view the compare list), real checkout was debugged end-to-end (5 stacked bugs, all fixed), the admin **Orders panel was rebuilt into a full ops console** (filters, bulk actions, SLA/high-value flags, status timeline, print/export), **testimonials** (homepage slider + section background + the standalone testimonials.html grid) are now admin-managed the same way banners are, the admin **Support Tickets page was rebuilt into a seller-ops queue** (ticket numbers, status/priority filters+search, a detail drawer, internal notes), a full **WhatsApp Auto-Reply bot was architected (plan-mode approved) and built** — Meta Cloud API webhook, live order/refund/product lookups, ticket hand-off into the same Support queue, buttons/lists instead of numbered text — the admin **Inventory page was rebuilt into a stock console** (search/category/stock-status filters with live counts, a real adjustment-history drawer wired to a previously-dead endpoint, CSV export), and then given **per-SKU low-stock thresholds + a real WhatsApp alert** on top of that (reusing the bot's own send wrapper, not a second integration). the long-broken **admin Customers page was actually fixed** (not the schema — a dead duplicate router had been squatting on `/api/customers` since some unknown point after Module 7 was first built, 404ing both `CustomersPage.tsx` and the Orders "New order" customer picker), all **5 storefront legal policy pages got real content** (Terms, Privacy, Return & Exchange, Shipping & Delivery, Cancellation) — replacing generic watches-template placeholder text with real India-law-compliant policy documents the user supplied, and a brand-new **Corporate & Compliance page** was added (`corporate-compliance.html`) with a sitewide footer-link rollout across all 30 other pages. See sections 8-19 above for the full breakdown. Sections 8-12 are on branch `claude/project-review-changes-yjarew`; sections 13-19 are on `claude/support-ticket-design-q3s0xb`, which also contains everything from 8-12 (branched from `yjarew`, not `main`). Neither branch is merged. **The WhatsApp bot (and therefore the low-stock alert) has no live credentials yet and has never sent a real message** — see Current Status and Next Priorities below before assuming it works.

### Current Status
- **phase8/9/10 applied** (confirmed live, 2026-08-11 — pasted directly into the Supabase SQL Editor rather than via `npm run migrate`, working around a local git-pull/CLI snag): `products.specifications`, `orders.payment_method`/`payment_status`, `customers.address`, and the `decrement_stock` RPC all exist now. Checkout then hit real bug #4 (see section 8, `customers_email_idx` duplicate-key) — **that fix is pushed (commit `473a604`) but needs `git pull` on the local machine before it's live**; no further migration needed for it, application code only.
- **✅ Confirmed working end-to-end, 2026-08-11**: a real order (`ELL-MSOMEJU6-325`) placed successfully through the storefront checkout after fixes #1-5, landing on `order-received.html` — which is what surfaced that page's own unwired-static-content problem, fixed the same day (see section 8).
- **phase11 NOT yet applied** (2026-08-11, section 9): `server/supabase/phase11_orders_ops.sql` (`orders.shipped_at`/`orders.delivered_at` + two indexes) hasn't been run against the live Supabase instance yet — the redesigned Orders admin page will error/500 on real data until this is run (`npm run migrate`, now that that script exists, or paste into the SQL Editor same as phase8/9/10 were).
- **phase12 NOT yet applied** (2026-08-12, section 10): `server/supabase/phase12_testimonials.sql` (`testimonials` table + `settings.testimonial_bg_image_url` + seed) hasn't been run either — until it is, `GET /api/public/testimonials` and `GET /api/public/settings` will 500, and the homepage/testimonials.html will silently keep showing their old static markup (both dynamic scripts `.catch()` failures the same way every other section in this codebase does).
- **phase13 NOT yet applied** (2026-08-14, section 13, branch `claude/support-ticket-design-q3s0xb` only): `server/supabase/phase13_support_tickets.sql` (`support_tickets.ticket_number`/`internal_note`/`resolved_at`) hasn't been run against the live Supabase instance yet, though it's now registered in `scripts/migrate.js` — run `npm run migrate` (or the SQL Editor) before testing the redesigned Support page. Until then `/support` will error since the code queries columns that don't exist in the live DB.
- **phase14 NOT yet applied** (2026-08-14, section 14, branch `claude/support-ticket-design-q3s0xb` only): `server/supabase/phase14_whatsapp.sql` (`wa_conversations`, `wa_messages`, `support_tickets.source`/`wa_conversation_id`) hasn't been run, though registered in `scripts/migrate.js`. **Also not yet done**: no WhatsApp Business API credentials exist anywhere (`WHATSAPP_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_VERIFY_TOKEN`/`WHATSAPP_APP_SECRET` are all blank in `server/.env`) — the bot is architecturally complete but has never received a real message. See section 14's last bullet for the actual first test steps (Meta test number + ngrok).
- **phase15 NOT yet applied** (2026-08-14, section 16, branch `claude/support-ticket-design-q3s0xb` only): `server/supabase/phase15_inventory_automation.sql` (`product_variants.low_stock_threshold`, `settings.low_stock_alert_phone`) hasn't been run, though registered in `scripts/migrate.js`. The low-stock WhatsApp alert additionally needs the same WhatsApp credentials as phase14 above **and** a phone number entered on the Settings page before it can send anything — until then it silently no-ops (by design).
- **Storefront Compilation**: The storefront can be compiled into a production-ready `dist` folder using the custom `node scripts/build-html.js` command.

### Next Priorities
1. **Merge `claude/project-review-changes-yjarew` into `main`** once verified locally (or keep developing on it — just don't lose track of which branch has the latest work).
2. **Run phase11 and phase12** (`npm run migrate` or the SQL Editor) before testing the new Orders ops console or the new Testimonials admin page — see sections 9-10 and Current Status above.
3. **Run the phase8 migration**, then in admin add 2-3 specification rows to a couple of real products and confirm they render on that product's storefront Additional Information tab.
4. **Cloudinary Integration**: Set up Cloudinary keys (`CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` in `server/.env`) to enable product and banner image uploads in the media library.
5. **Razorpay Secure Checkout**: Implement and test the Razorpay payment gateway to replace the current Cash-on-Delivery (COD) checkout method.
6. **Reviews feature**: product-single.html's Reviews tab is now honest ("No reviews yet") instead of fake, but there's still no real reviews table/submission backend — build one if customer reviews are actually wanted.
7. ~~`order-received.html` is still static/unwired to real order data~~ **Fixed 2026-08-11** — see section 8. Both this and `account-order-details.html` are now wired to real orders.
8. ~~**Admin CRM customers endpoint missing**: `CustomersPage.tsx` and the Orders "New order" dialog both call `GET /api/customers`, but that path is actually the storefront customer-account router — there's no real admin customer-list-with-stats endpoint anywhere (see the "Known pre-existing gap" note in section 9). Needs a real `GET /api/crm/customers` (or similar) built from the existing `loadCustomersWithStats()` helper already in `server/routes/crm.js`, and both admin pages repointed to it.~~ **Fixed 2026-08-14 — see section 17.**
9. **Run phase13** (`npm run migrate` or the SQL Editor) before testing the redesigned Support Tickets page — see section 13 and Current Status above. It's on `claude/support-ticket-design-q3s0xb` only, not on `yjarew` or `main`.
10. **Reconcile the two unmerged branches**: `claude/support-ticket-design-q3s0xb` already contains all of `claude/project-review-changes-yjarew`'s commits plus the Support Tickets work, so merging *it* into `main` (once verified) would bring in sections 8-13 together — simpler than merging both branches separately. Also worth a quick look at the third origin branch, `feat/phase6-modules`, which nothing in this file currently accounts for.
11. **Get the WhatsApp bot actually live** (section 14): run phase14, register a Meta for Developers app + WhatsApp product, grab the free test number, fill in `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_VERIFY_TOKEN`/`WHATSAPP_APP_SECRET` in `server/.env`, point the webhook at a local tunnel (ngrok), and walk all 10 root menu branches from a real phone. Nothing in section 14 has been tested against a real WhatsApp message yet.
12. **Run phase15 and set a low-stock alert number** (section 16): once WhatsApp credentials exist (item 11), run `server/supabase/phase15_inventory_automation.sql`, enter a real number on the Settings page, then trigger a stock adjustment that crosses the threshold and confirm the alert actually arrives.
13. **Fill in the real bracketed placeholders on the 5 policy pages** (section 18): corporate office address, customer support phone number, and governing-law jurisdiction city are still shown as visible `[Insert ...]` brackets on `term-condition.html`/`privacy-policy.html`/`refund-return.html`/`delivery-policy.html`/`cancellation-policy.html` — swap in the real values once known, same way this file's own contact fields get filled in elsewhere.

Local dev: `npm run install:all` (root) once per machine, then `npm run dev:full` (root) runs storefront + Express API + admin Vite dev server together. Test login: `admin@ellora.test` / `EllroaAdmin@2026`.